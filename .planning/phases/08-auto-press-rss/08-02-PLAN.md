---
phase: 08-auto-press-rss
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/api/cron/auto-press/route.ts
  - src/app/api/netpro/list/route.ts
  - src/app/api/netpro/detail/route.ts
  - src/app/api/netpro/image/route.ts
  - src/middleware.ts
  - src/app/cam/auto-press/page.tsx
autonomous: true
requirements: [RSS-03, RSS-05]
must_haves:
  truths:
    - "fetchNetproList, fetchNetproDetail 함수와 관련 인터페이스가 route.ts에서 완전히 제거된다"
    - "netpro API 라우트(list, detail, image)가 삭제되어 404를 반환한다"
    - "runAutoPress()에서 넷프로 분기 코드가 제거되고 모든 소스가 RSS 경로로 처리된다"
    - "어드민 auto-press 페이지에서 netpro 옵션이 제거된다"
    - "pnpm build 성공 후 vercel deploy --prod 배포 완료"
  artifacts:
    - path: "src/app/api/cron/auto-press/route.ts"
      provides: "넷프로 코드 완전 제거된 auto-press 핸들러"
    - path: "src/app/cam/auto-press/page.tsx"
      provides: "netpro 옵션 제거된 어드민 UI"
  key_links:
    - from: "src/app/api/cron/auto-press/route.ts"
      to: "src/lib/newswire-extract.ts"
      via: "import (Plan 01에서 추가)"
      pattern: "extractNewswireArticle"
    - from: "src/app/api/cron/auto-press/route.ts"
      to: "fetchRssFeed + fetchOriginContent"
      via: "모든 소스가 RSS 경로로 통합"
      pattern: "fetchRssFeed.*rssUrl"
---

<objective>
넷프로 경유 코드를 완전히 제거하고, auto-press가 순수 RSS 직접 수집만 사용하도록 전환한다. netpro API 라우트를 삭제하고 어드민 UI를 정리한 뒤 빌드/배포한다.

Purpose: 넷프로 2단계 프록시(self-fetch -> netpro.kr 스크래핑)를 제거하여 수집 안정성과 성능을 개선하고, 불필요한 코드를 정리한다.
Output: 넷프로 코드가 완전히 제거된 auto-press 시스템, 배포 완료
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-auto-press-rss/08-RESEARCH.md
@.planning/phases/08-auto-press-rss/08-01-SUMMARY.md

@src/app/api/cron/auto-press/route.ts
@src/middleware.ts
@src/app/cam/auto-press/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: route.ts 넷프로 코드 완전 제거 + RSS 전용화</name>
  <files>src/app/api/cron/auto-press/route.ts</files>
  <action>
1. 넷프로 관련 코드 삭제 (route.ts):
   - `NetproListItem` 인터페이스 (207행 근처) 삭제
   - `fetchNetproList()` 함수 (216행 근처) 삭제
   - `NetproDetail` 인터페이스 (239행 근처) 삭제
   - `fetchNetproDetail()` 함수 (249행 근처) 삭제
   - 파일 상단 주석에서 "netpro" 언급 제거

2. `PressTarget` 인터페이스 정리 (398행 근처):
   - `item` 필드의 타입을 `NetproListItem`에서 인라인 타입 또는 새 `RssTarget` 인터페이스로 변경:
     ```typescript
     interface RssTarget {
       id: string;      // URL 기반 base64 고유키
       title: string;
       date: string;
       link: string;    // 원문 URL
     }
     interface PressTarget {
       item: RssTarget;
       source: AutoPressSource;
     }
     ```

3. `runAutoPress()` 수집 로직 단순화 (414행 근처):
   - `netproSources` / `rssSources` 분기 제거
   - 모든 소스를 RSS로 처리:
     ```typescript
     const allItems: PressTarget[] = [];
     const rssResults = await Promise.all(
       activeSources.filter(s => s.rssUrl).map(async (source) => {
         const rssItems = await fetchRssFeed(source.rssUrl!, Math.ceil(count * 3));
         return rssItems.map(rssItem => ({
           item: {
             id: Buffer.from(rssItem.link).toString("base64url").slice(0, 40),
             title: rssItem.title,
             date: rssItem.pubDate ? new Date(rssItem.pubDate).toISOString().slice(0, 10) : "",
             link: rssItem.link,
           },
           source,
         }));
       })
     );
     for (const items of rssResults) allItems.push(...items);
     ```

4. 상세 수집 분기 단순화 (500행 근처):
   - netpro 분기 제거, 모든 항목이 `fetchOriginContent()` 사용:
     ```typescript
     const origin = await fetchOriginContent(baseUrl, target.item.link);
     if (origin) {
       detail = {
         title: origin.title || item.title,
         bodyText: origin.bodyText,
         bodyHtml: origin.bodyHtml,
         date: origin.date || item.date,
         writer: "",
         images: origin.images,
         sourceUrl: origin.sourceUrl || item.link,
       };
     }
     ```

5. `isDuplicate()` 함수의 `wrId`/`boTable` 파라미터를 `id`로 단순화 (이력 호환 유지)

6. 결과 기록(`results.push`)에서 `boTable` 필드를 소스 이름이나 빈 문자열로 대체

주의:
- `wrIds` 옵션 (특정 기사 지정) 기능도 RSS 방식으로 전환
- preview 모드 로직 유지
- 타임아웃 체크, 금칙어 필터, 이미지 필수 체크 등 기존 로직 모두 유지
- `addToDbCache()` 호출 유지
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>route.ts에서 fetchNetproList, fetchNetproDetail, NetproListItem, NetproDetail이 모두 제거되고, 모든 소스가 RSS 경로로 처리되며, 타입 에러가 없다</done>
</task>

<task type="auto">
  <name>Task 2: netpro API 삭제 + 미들웨어/어드민 UI 정리 + 빌드/배포</name>
  <files>src/app/api/netpro/list/route.ts, src/app/api/netpro/detail/route.ts, src/app/api/netpro/image/route.ts, src/middleware.ts, src/app/cam/auto-press/page.tsx</files>
  <action>
1. netpro API 라우트 삭제:
   - `src/app/api/netpro/list/route.ts` 삭제
   - `src/app/api/netpro/detail/route.ts` 삭제
   - `src/app/api/netpro/image/route.ts` 삭제
   - `src/app/api/netpro/origin/route.ts`는 유지 (범용 원문 추출 기능, press-import 페이지에서 사용 가능)
   - 빈 디렉토리가 되면 디렉토리도 정리 (origin만 남으므로 디렉토리 유지)

2. `src/middleware.ts` 정리:
   - 155행: `/api/netpro` 경로 보호 로직에서 제거 — 단, origin 라우트가 남으므로 유지해야 할 수도 있음
   - 실제로 origin이 남으므로 `/api/netpro` 매처는 유지 (origin 보호 필요)
   - 변경 없음 (origin이 남아있으므로)

3. `src/app/cam/auto-press/page.tsx` 정리:
   - `newSourceFetchType` 상태와 관련 select 드롭다운 제거 (139행, 501~505행)
   - 새 소스 추가 시 `fetchType`을 항상 `"rss"`로 고정
   - 소스 목록 표시에서 "netpro"/"뉴스와이어" 구분 배지 제거 → 모두 "RSS" 표시
   - fetchType 관련 select UI 제거, rssUrl 입력만 유지
   - DEFAULT_SOURCES 배열에서 netpro 소스 제거 (이미 auto-defaults.ts에서 제거됨, 로컬 복사본도 동기화)

4. 빌드 및 배포:
   - `pnpm build` 실행하여 타입 에러/import 에러 없음 확인
   - `vercel deploy --prod` 실행

주의:
- netpro/origin은 삭제하지 않음 (press-import 페이지에서 범용 원문 추출로 사용)
- 미들웨어의 /api/netpro 매처는 origin 보호를 위해 유지
- 어드민 UI의 기존 소스 목록은 DB에서 로드되므로 화면 표시만 정리
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>netpro list/detail/image API가 삭제되고, 어드민 UI에서 netpro 옵션이 제거되며, pnpm build 성공 후 vercel deploy --prod 배포가 완료된다</done>
</task>

</tasks>

<verification>
1. `pnpm build` 성공
2. `grep -r "fetchNetpro" src/` 결과 0건
3. `ls src/app/api/netpro/` — origin만 남음 (list, detail, image 삭제됨)
4. auto-press 페이지에서 소스 추가 시 fetchType 선택 UI 없음
5. vercel deploy --prod 성공
</verification>

<success_criteria>
- fetchNetproList, fetchNetproDetail 함수가 코드에서 완전히 제거되었다
- netpro API 3개 라우트(list, detail, image)가 삭제되었다
- 어드민 auto-press 페이지에서 "netpro" 옵션이 제거되었다
- pnpm build가 에러 없이 성공한다
- vercel deploy --prod가 완료된다
</success_criteria>

<output>
완료 후 `.planning/phases/08-auto-press-rss/08-02-SUMMARY.md` 생성
</output>
