---
phase: 02-public-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/category/[slug]/page.tsx
  - src/app/article/[id]/page.tsx
autonomous: true
requirements: [PUB-01, PUB-02, PUB-03]

must_haves:
  truths:
    - "카테고리 페이지에서 전체 기사(3000건)가 아닌 인기 기사 10건만 사이드바용으로 조회된다"
    - "기사 상세 breadcrumb의 카테고리 링크가 URL-safe하게 인코딩된다"
    - "홈/기사상세/카테고리 페이지가 pnpm build로 정상 빌드된다"
  artifacts:
    - path: "src/app/category/[slug]/page.tsx"
      provides: "카테고리 페이지 최적화"
      contains: "serverGetTopArticles"
    - path: "src/app/article/[id]/page.tsx"
      provides: "기사 상세 breadcrumb 인코딩"
      contains: "encodeURIComponent"
  key_links:
    - from: "category/[slug]/page.tsx"
      to: "serverGetTopArticles(10)"
      via: "사이드바 인기 기사 조회"
      pattern: "serverGetTopArticles"
    - from: "article/[id]/page.tsx breadcrumb"
      to: "/category/ URL"
      via: "encodeURIComponent"
      pattern: "encodeURIComponent.*category"
---

<objective>
카테고리 페이지의 불필요한 전체 기사 로드를 최적화하고, 기사 상세 breadcrumb 카테고리 링크를 안전하게 수정한다. 홈/기사상세/카테고리 3개 페이지가 정상 작동함을 빌드로 확인한다.

Purpose: 카테고리 페이지 ISR 재생성 시 Supabase 불필요 호출 제거, 기사 상세 페이지 방어적 코딩 보완
Output: 수정된 category/[slug]/page.tsx, article/[id]/page.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-public-pages/02-RESEARCH.md

<interfaces>
<!-- 이미 존재하는 함수 — 신규 코드 작성 불필요 -->

From src/lib/db-server.ts:
```typescript
export async function serverGetTopArticles(limit?: number): Promise<Article[]>;
// 기본값 limit=10, 조회수 기준 상위 N건, status=게시 필터 포함
```

From src/lib/supabase-server-db.ts:
```typescript
export async function sbGetTopArticles(limit?: number): Promise<Article[]>;
// views.desc 정렬, 게시 상태만 조회
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 카테고리 페이지 allArticles 최적화 + 미사용 searchParams 정리 (BUG-02, BUG-04)</name>
  <files>src/app/category/[slug]/page.tsx</files>
  <read_first>src/app/category/[slug]/page.tsx, src/lib/db-server.ts (serverGetTopArticles 함수 확인)</read_first>
  <action>
카테고리 페이지에서 두 가지를 수정한다.

**1. allArticles 전체 로드 → serverGetTopArticles(10) 교체 (BUG-02)**

현재 (라인 79):
```typescript
const allArticles = (siteType === "insightkorea" || siteType === "culturepeople") ? await serverGetArticles() : [];
```

변경:
```typescript
const allArticles = (siteType === "insightkorea" || siteType === "culturepeople") ? await serverGetTopArticles(10) : [];
```

- `serverGetTopArticles(10)`은 이미 `db-server.ts`에 존재하며, `sbGetTopArticles(limit)` → `status=eq.게시&order=views.desc&limit=10`으로 DB 레벨에서 10건만 조회
- 테마 컴포넌트(`CulturePeopleCategoryPage`)에서 `allArticles`는 사이드바 "인기 기사" 10건 표시에만 사용 → 전체 3000건을 가져와 클라이언트에서 sort/slice할 필요 없음
- import 변경: `serverGetArticles` import를 `serverGetTopArticles`로 교체 (serverGetArticles가 이 파일에서 더 이상 사용되지 않으면 import에서 제거)

**2. 미사용 searchParams 제거 (BUG-04)**

현재 Props 인터페이스 (라인 20-22):
```typescript
interface Props {
  params: Promise<{ slug: string }>;
  searchParams: Promise<{ page?: string }>;
}
```

변경:
```typescript
interface Props {
  params: Promise<{ slug: string }>;
}
```

- `searchParams`는 `CategoryPage` 함수에서 사용하지 않으므로 제거
- 함수 시그니처에서도 searchParams 관련 부분이 있으면 제거
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - category/[slug]/page.tsx에 `serverGetArticles()` 호출이 없음 (serverGetArticles import 제거 또는 미사용)
    - `serverGetTopArticles(10)` 호출이 존재함
    - Props 인터페이스에 `searchParams`가 없음
    - TypeScript 컴파일 에러 없음
  </acceptance_criteria>
  <done>카테고리 페이지가 사이드바용 기사 10건만 DB에서 조회하고, 미사용 searchParams가 제거됨</done>
</task>

<task type="auto">
  <name>Task 2: 기사 상세 breadcrumb 카테고리 URL 인코딩 + 빌드 검증 (BUG-07)</name>
  <files>src/app/article/[id]/page.tsx</files>
  <read_first>src/app/article/[id]/page.tsx (라인 200-220 부근 breadcrumb 확인)</read_first>
  <action>
**1. breadcrumb 카테고리 URL 인코딩 (BUG-07)**

netpro 테마 전용 breadcrumb (약 라인 212):

변경 전:
```typescript
<Link href={`/category/${article.category}`} ...>
```

변경 후:
```typescript
<Link href={`/category/${encodeURIComponent(article.category || "")}`} ...>
```

- 현재 카테고리명은 "문화", "경제" 등 한글 단어라 실제 문제는 없지만, 향후 "문화/예술" 같은 카테고리 추가 시 URL 깨짐 방지
- `article.category`가 null/undefined일 수 있으므로 `|| ""` 폴백 추가
- 파일 내 다른 위치에도 같은 패턴이 있으면 함께 수정 (grep으로 `/category/${article.category}` 패턴 확인)

**2. 전체 빌드 검증**

모든 수정 후 `pnpm build`를 실행하여 홈/기사상세/카테고리/태그/검색 페이지가 정상 빌드되는지 확인한다.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && pnpm build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - article/[id]/page.tsx breadcrumb에서 `encodeURIComponent`가 category에 적용됨
    - pnpm build가 에러 없이 완료됨
    - 빌드 출력에 홈(/) , 기사(/article), 카테고리(/category), 태그(/tag), 검색(/search) 페이지가 포함됨
  </acceptance_criteria>
  <done>기사 상세 breadcrumb이 URL-safe하고, 전체 프로젝트가 빌드 성공한다</done>
</task>

</tasks>

<verification>
pnpm build가 에러 없이 완료된다.
category/[slug]/page.tsx에 serverGetArticles() 직접 호출이 없다.
article/[id]/page.tsx breadcrumb에 encodeURIComponent가 적용되어 있다.
</verification>

<success_criteria>
1. 카테고리 페이지 ISR 재생성 시 Supabase에 전체 기사 대신 인기 기사 10건만 요청한다
2. 기사 상세 breadcrumb 카테고리 링크가 특수문자에 안전하다
3. 전체 프로젝트가 pnpm build에 성공한다
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-pages/02-02-SUMMARY.md`
</output>
