# Phase 20-02 Plan: Admin UX, runbook, and closure

**Status:** Complete 2026-05-25.

## Goal

Make SMTP credential management understandable and safe for operators, then close v4.0 with tests, documentation, and production-safe validation.

## Tasks

1. Harden admin UX.
   - Show when SMTP credentials are environment-managed.
   - Prevent masked placeholders from overwriting environment-managed secrets.
   - Keep non-secret sender/reply-to metadata editable where safe.

2. Add operator documentation.
   - Document required Vercel environment variables.
   - Explain DB fallback behavior and migration steps.
   - Include smoke checks for SMTP test and newsletter send.

3. Close validation.
   - Add or update tests for admin settings masking/status behavior.
   - Run planning guard, lint, typecheck, unit tests, audit, and build.
   - Record v4.0 closure status.

## Non-Goals

- Do not perform a live SMTP send without explicit operator intent.
- Do not rotate production credentials automatically.
- Do not include broader Cloudflare runtime cutover work.

## Acceptance Criteria

- [x] Admin screens make env-managed SMTP state clear without exposing secrets.
- [x] Runbook covers Vercel env setup and fallback removal.
- [x] Full validation passes on the Linux working tree.

## Verification

- Added safe `smtpRuntimeStatus` to the admin settings response without exposing raw SMTP passwords.
- Protected env-managed SMTP fields on settings save so masked placeholders and stale UI values do not overwrite DB fallback values.
- Updated `/cam/settings` and `/cam/newsletter` to display SMTP runtime status and lock env-managed fields.
- Added `docs/smtp-credential-runbook.md`.
- Added `tests/unit/settings-route-smtp.test.ts` for masked settings response and env-managed save protection.
- Strengthened `pnpm check:planning` to fail if v4.0 is complete but requirements or plans remain pending.
- Passed full local `pnpm ci:all` on the Linux working tree.
