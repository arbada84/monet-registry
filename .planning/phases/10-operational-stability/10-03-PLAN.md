---
phase: 10-operational-stability
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/lib/supabase-server-db.ts
  - src/lib/db-server.ts
  - src/app/api/db/articles/route.ts
autonomous: true
requirements:
  - PERF-02
must_haves:
  truths:
    - "어드민 기사 목록 API가 DB 레벨에서 필터링(status/category/검색어)하여 4000+건 전체 조회 없이 필요한 페이지만 반환한다"
    - "필터링 결과의 총 개수(total)가 Prefer: count=exact 헤더로 DB에서 직접 반환된다"
    - "비인증 요청은 게시 상태만 DB 레벨에서 필터링된다"
  artifacts:
    - path: "src/lib/supabase-server-db.ts"
      provides: "sbGetFilteredArticles 함수"
      contains: "sbGetFilteredArticles"
    - path: "src/lib/db-server.ts"
      provides: "serverGetFilteredArticles 래퍼"
      contains: "serverGetFilteredArticles"
    - path: "src/app/api/db/articles/route.ts"
      provides: "DB 레벨 필터링 적용된 GET 핸들러"
      contains: "serverGetFilteredArticles"
  key_links:
    - from: "src/app/api/db/articles/route.ts"
      to: "src/lib/db-server.ts"
      via: "serverGetFilteredArticles({ q, category, status, page, limit })"
      pattern: "serverGetFilteredArticles"
    - from: "src/lib/db-server.ts"
      to: "src/lib/supabase-server-db.ts"
      via: "dynamic import sbGetFilteredArticles"
      pattern: "sbGetFilteredArticles"
---

<objective>
/api/db/articles GET 핸들러의 클라이언트 사이드 필터링(JS filter)을 Supabase DB 레벨 필터링(ilike, eq)으로 전환한다.

Purpose: 4000+건을 메모리에 로드 후 JS 필터링하는 방식 대신 DB에서 필터링하여 응답 속도/메모리 크게 개선
Output: sbGetFilteredArticles 함수 + route.ts 리팩토링
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-operational-stability/10-RESEARCH.md
@.planning/phases/10-operational-stability/10-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 함수들 + 기존 패턴 -->
From src/lib/supabase-server-db.ts:
```typescript
const BASE_URL = process.env.NEXT_PUBLIC_SUPABASE_URL;
function getHeaders(write = false): Record<string, string>
function rowToArticle(r: Record<string, unknown>, includeBody = true): Article
// 기존 baseSelect (body 제외):
// "id,no,title,category,date,status,views,thumbnail,thumbnail_alt,tags,author,author_email,summary,slug,meta_description,og_image,scheduled_publish_at,updated_at,created_at,source_url,aiGenerated"
```

From src/lib/db-server.ts:
```typescript
export async function serverGetArticles(): Promise<Article[]>  // 폴백으로 남아있음
```

From src/app/api/db/articles/route.ts (현재 — 변경 대상):
```typescript
// 50행: let articles = await serverGetArticles();
// 54-56행: if (!authed) articles = articles.filter(a => a.status === "게시");
// 58-74행: q/category/status JS 필터링
// 76행: const total = articles.length;
// 78-87행: 페이지네이션 (JS slice)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: sbGetFilteredArticles 함수 생성 + db-server.ts 래퍼</name>
  <files>src/lib/supabase-server-db.ts, src/lib/db-server.ts</files>
  <read_first>
    - src/lib/supabase-server-db.ts (getHeaders, rowToArticle, BASE_URL, baseSelect 패턴 확인)
    - src/lib/db-server.ts (기존 래퍼 패턴 확인)
    - src/app/api/db/articles/route.ts (현재 필터링 로직의 정확한 동작 확인 — q/category/status/trash/page/limit/authed)
  </read_first>
  <action>
**supabase-server-db.ts에 sbGetFilteredArticles 추가:**

```typescript
export async function sbGetFilteredArticles(opts: {
  q?: string;
  category?: string;
  status?: string;
  page?: number;
  limit?: number;
  includeDeleted?: boolean;
  authed?: boolean;
}): Promise<{ articles: Article[]; total: number }> {
  const select = "id,no,title,category,date,status,views,thumbnail,thumbnail_alt,tags,author,author_email,summary,slug,updated_at,created_at,source_url,aiGenerated";
  const filters: string[] = [];

  // 삭제되지 않은 기사만 (기본)
  if (!opts.includeDeleted) {
    filters.push("deleted_at=is.null");
  }

  // 비인증 요청: 게시 상태만
  if (!opts.authed) {
    filters.push(`status=eq.${encodeURIComponent("게시")}`);
  } else if (opts.status) {
    filters.push(`status=eq.${encodeURIComponent(opts.status)}`);
  }

  if (opts.category) {
    filters.push(`category=eq.${encodeURIComponent(opts.category)}`);
  }

  if (opts.q) {
    const q = opts.q.trim();
    if (q) {
      const encoded = encodeURIComponent(q);
      // ilike 와일드카드는 인코딩하지 않음
      filters.push(`or=(title.ilike.*${encoded}*,author.ilike.*${encoded}*,tags.ilike.*${encoded}*)`);
    }
  }

  const limit = opts.limit || 20;
  const offset = ((opts.page || 1) - 1) * limit;
  const filterStr = filters.length ? `&${filters.join("&")}` : "";

  const url = `${BASE_URL}/rest/v1/articles?select=${select}${filterStr}&order=date.desc,created_at.desc&limit=${limit}&offset=${offset}`;
  const res = await fetch(url, {
    headers: { ...getHeaders(false), "Prefer": "count=exact" },
    cache: "no-store",
  });

  if (!res.ok) {
    const errText = await res.text().catch(() => "");
    throw new Error(`Supabase filtered articles error ${res.status}: ${errText.slice(0, 200)}`);
  }

  // content-range 헤더에서 총 개수 추출: "0-19/4235" 또는 "*/0"
  const range = res.headers.get("content-range");
  let total = 0;
  if (range) {
    const parts = range.split("/");
    if (parts[1] && parts[1] !== "*") total = parseInt(parts[1], 10);
  }

  const rows = (await res.json()) as Record<string, unknown>[];
  return { articles: rows.map(r => rowToArticle(r, false)), total };
}
```

**db-server.ts에 래퍼 추가:**

```typescript
export async function serverGetFilteredArticles(opts: {
  q?: string; category?: string; status?: string;
  page?: number; limit?: number;
  includeDeleted?: boolean; authed?: boolean;
}): Promise<{ articles: Article[]; total: number }> {
  if (isSupabaseEnabled()) {
    try {
      const { sbGetFilteredArticles } = await import("@/lib/supabase-server-db");
      return await sbGetFilteredArticles(opts);
    } catch { /* 폴백 */ }
  }
  // 폴백: 전체 조회 후 JS 필터링
  let articles = await serverGetArticles();
  if (!opts.authed) articles = articles.filter(a => a.status === "게시");
  if (opts.status) articles = articles.filter(a => a.status === opts.status);
  if (opts.category) articles = articles.filter(a => a.category === opts.category);
  if (opts.q) {
    const q = opts.q.toLowerCase();
    articles = articles.filter(a =>
      a.title.toLowerCase().includes(q) ||
      a.author?.toLowerCase().includes(q) ||
      a.tags?.toLowerCase().includes(q)
    );
  }
  const total = articles.length;
  const limit = opts.limit || 20;
  const offset = ((opts.page || 1) - 1) * limit;
  return { articles: articles.slice(offset, offset + limit), total };
}
```
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && grep -c "sbGetFilteredArticles" src/lib/supabase-server-db.ts && grep -c "serverGetFilteredArticles" src/lib/db-server.ts && pnpm build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export async function sbGetFilteredArticles" src/lib/supabase-server-db.ts` 결과 1건
    - `grep "export async function serverGetFilteredArticles" src/lib/db-server.ts` 결과 1건
    - `grep "count=exact" src/lib/supabase-server-db.ts` 결과 1건 이상
    - `grep "content-range" src/lib/supabase-server-db.ts` 결과 1건 이상
    - `grep "ilike" src/lib/supabase-server-db.ts` 결과 1건 이상
    - `pnpm build` 성공
  </acceptance_criteria>
  <done>sbGetFilteredArticles 함수와 db-server.ts 래퍼가 생성되어 DB 레벨 필터링+페이지네이션+카운트 지원, 빌드 성공</done>
</task>

<task type="auto">
  <name>Task 2: /api/db/articles GET 핸들러를 serverGetFilteredArticles로 전환</name>
  <files>src/app/api/db/articles/route.ts</files>
  <read_first>
    - src/app/api/db/articles/route.ts (GET 핸들러 전체 — 50-95행의 필터링/페이지네이션 로직)
    - src/lib/db-server.ts (Task 1에서 추가한 serverGetFilteredArticles 시그니처 확인)
  </read_first>
  <action>
src/app/api/db/articles/route.ts의 GET 핸들러를 리팩토링.

**변경 전 (50-95행):**
```typescript
let articles = await serverGetArticles();
if (!authed) articles = articles.filter(...);
// q, category, status JS 필터링
const total = articles.length;
// JS slice 페이지네이션
```

**변경 후:**
```typescript
// import 추가: serverGetFilteredArticles
import {
  serverGetArticleById,
  serverCreateArticle,
  serverUpdateArticle,
  serverDeleteArticle,
  serverGetSetting,
  serverGetFilteredArticles,
} from "@/lib/db-server";

// GET 핸들러 내부 (id 조회와 trash 조회 이후):
const sp = request.nextUrl.searchParams;
const q = sp.get("q")?.trim().slice(0, 200) || undefined;
const category = sp.get("category") || undefined;
const status = sp.get("status") || undefined;
const pageParam = sp.get("page");
const limitParam = sp.get("limit");
const maxLimit = authed ? 10000 : 200;
const limit = Math.min(maxLimit, Math.max(1, parseInt(limitParam ?? "20", 10)));
const page = Math.max(1, parseInt(pageParam ?? "1", 10));

const { articles, total } = await serverGetFilteredArticles({
  q, category, status, page, limit, authed,
});

const cacheControl = authed
  ? "private, no-cache"
  : "public, s-maxage=60, stale-while-revalidate=300";

if (pageParam || limitParam) {
  return NextResponse.json({ success: true, articles, total, page, limit }, {
    headers: { "Cache-Control": cacheControl },
  });
}
return NextResponse.json({ success: true, articles, total }, {
  headers: { "Cache-Control": cacheControl },
});
```

**기존 import에서 `serverGetArticles` 제거** (이 파일에서 더 이상 사용하지 않으므로).

**주의:** id 단건 조회 (25-28행)와 trash 조회 (44-48행)는 기존 로직 유지. 변경은 50행부터의 목록 조회 부분만.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && grep "serverGetArticles()" src/app/api/db/articles/route.ts | wc -l && grep -c "serverGetFilteredArticles" src/app/api/db/articles/route.ts && pnpm build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep "serverGetArticles()" src/app/api/db/articles/route.ts` 결과 0건 (전체 조회 제거)
    - `grep "serverGetFilteredArticles" src/app/api/db/articles/route.ts` 결과 2건 이상 (import + 호출)
    - `grep "\.filter(" src/app/api/db/articles/route.ts` 결과 0건 (JS 필터링 제거)
    - `pnpm build` 성공
  </acceptance_criteria>
  <done>/api/db/articles GET이 DB 레벨 필터링으로 전환되어 전체 조회+JS 필터링 코드 제거, 빌드 성공</done>
</task>

</tasks>

<verification>
- `pnpm build` 성공
- `grep "serverGetArticles()" src/app/api/db/articles/route.ts` 결과 0건
- `grep "serverGetFilteredArticles" src/app/api/db/articles/route.ts` 결과 2건 이상
- `grep "\.filter(" src/app/api/db/articles/route.ts` 결과 0건
</verification>

<success_criteria>
1. 어드민 기사 목록 API가 DB 레벨에서 필터링하여 전체 조회 없이 필요한 페이지만 반환
2. 총 개수(total)가 DB에서 count=exact로 직접 계산
3. 비인증 요청은 DB 레벨에서 게시 상태만 반환
4. 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/10-operational-stability/10-03-SUMMARY.md`
</output>
