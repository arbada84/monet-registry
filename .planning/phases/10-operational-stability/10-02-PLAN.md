---
phase: 10-operational-stability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/middleware.ts
  - src/app/api/db/comments/route.ts
  - src/app/api/auth/login/route.ts
  - .gitignore
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - CLEAN-01
must_haves:
  truths:
    - "인메모리 rate limit(commentRateMap, cronRateLimitMap, memAttempts) 코드가 완전히 제거되어 Redis만 사용한다"
    - "모든 인증 쿠키가 secure: true로 하드코딩되어 환경 조건 분기가 없다"
    - "루트 디렉토리에 temp/tmp/cookies.txt/nul 파일이 없고 git 추적도 해제되었다"
  artifacts:
    - path: "src/middleware.ts"
      provides: "Redis 전용 cron rate limit"
      contains: "redisCheckRateLimit"
    - path: "src/app/api/db/comments/route.ts"
      provides: "Redis 전용 댓글 rate limit"
      contains: "redisCheckRateLimit"
    - path: "src/app/api/auth/login/route.ts"
      provides: "Redis 전용 로그인 rate limit + secure cookie"
      contains: "secure: true"
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/redis.ts"
      via: "import { checkRateLimit as redisCheckRateLimit }"
      pattern: "redisCheckRateLimit"
    - from: "src/app/api/auth/login/route.ts"
      to: "src/lib/redis.ts"
      via: "import { redis, checkRateLimit }"
      pattern: "checkRateLimit"
---

<objective>
인메모리 rate limit 잔여분 3곳을 Redis 전용으로 전환하고, cookie secure 플래그를 하드코딩하고, temp 파일을 정리한다.

Purpose: 서버리스 인스턴스 간 rate limit 일관성 보장 + 환경 무관 쿠키 보안 + 불필요한 파일 제거
Output: 3개 파일의 인메모리 폴백 제거, cookie secure 하드코딩, temp 파일 삭제 및 git 추적 해제
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-operational-stability/10-RESEARCH.md

<interfaces>
From src/lib/redis.ts:
```typescript
export let redis: InstanceType<typeof Redis> | null;
export async function checkRateLimit(
  ip: string, prefix: string, maxPerWindow: number, windowSeconds: number
): Promise<boolean>  // redis 없으면 true 반환 (가용성 우선)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 인메모리 rate limit 3곳 Redis 전용 전환 (SEC-01)</name>
  <files>src/middleware.ts, src/app/api/db/comments/route.ts, src/app/api/auth/login/route.ts</files>
  <read_first>
    - src/middleware.ts (cronRateLimitMap 선언 9행~, checkCronRateLimit 함수 전체)
    - src/app/api/db/comments/route.ts (commentRateMap 65행~, checkCommentRateLimit 함수 전체)
    - src/app/api/auth/login/route.ts (memAttempts 43행~, checkRateLimit/recordFailure/clearAttempts 함수 전체)
    - src/lib/redis.ts (checkRateLimit 시그니처와 redis export 확인)
  </read_first>
  <action>
**1. src/middleware.ts — cronRateLimitMap 제거:**
- `cronRateLimitMap` Map 선언 (9행 부근) 삭제
- `checkCronRateLimit(ip)` 함수를 아래로 교체:
```typescript
async function checkCronRateLimit(ip: string): Promise<boolean> {
  return redisCheckRateLimit(ip, "cp:cron:rate:", 5, 60);
}
```
- 기존 import에 `redis`가 있고 `checkRateLimit as redisCheckRateLimit`가 있는지 확인. 없으면 추가: `import { checkRateLimit as redisCheckRateLimit } from "@/lib/redis";` (이미 4행에 `import { redis, checkRateLimit as redisCheckRateLimit } from "@/lib/redis";` 있음)
- `redis` import는 다른 곳에서도 사용하므로 유지

**2. src/app/api/db/comments/route.ts — commentRateMap 제거:**
- `commentRateMap` Map 선언 (65행), `lastRateCleanup` 변수 (66행) 삭제
- `checkCommentRateLimit(ip)` 함수를 아래로 교체:
```typescript
async function checkCommentRateLimit(ip: string): Promise<boolean> {
  const allowed = await redisCheckRateLimit(ip, "cp:comment:rate:", COMMENT_LIMIT, 600);
  if (!allowed) {
    console.warn(`[security] 댓글 Rate Limit 초과: ip=${ip.slice(0, 8)}***`);
  }
  return allowed;
}
```
- `COMMENT_LIMIT` 상수 (63행, 값 5) 유지
- `COMMENT_WINDOW_MS` 상수 (64행) 삭제 (Redis 윈도우는 초 단위 600으로 대체)
- import에서 `redis` 제거 가능 여부 확인 — `redis`가 다른 곳에서 사용되지 않으면 제거. `redisCheckRateLimit`은 유지.

**3. src/app/api/auth/login/route.ts — memAttempts + 로컬 Redis 인스턴스 제거:**
- 27-38행의 로컬 Redis 인스턴스 생성 코드 제거 (파일 상단의 `let redis` + `new Redis(...)`)
- 대신 공통 redis.ts에서 import: `import { redis, checkRateLimit as redisCheckRateLimit } from "@/lib/redis";`
- `memAttempts` Map 선언 (43행), `MEM_MAX_SIZE` (42행) 삭제
- `getMemEntry()` 함수 (54-62행) 삭제
- `evictExpired()` 함수 (64-79행) 삭제
- `checkRateLimit(ip)` 함수에서 인메모리 폴백 제거:
```typescript
async function checkLoginRateLimit(ip: string): Promise<{ allowed: boolean; remainingMs?: number }> {
  if (!redis) return { allowed: true };
  try {
    const lockKey = `cp:login:lock:${ip}`;
    const ttl = await redis.ttl(lockKey);
    if (ttl > 0) return { allowed: false, remainingMs: ttl * 1000 };
    return { allowed: true };
  } catch {
    return { allowed: true };
  }
}
```
- 함수명을 `checkLoginRateLimit`로 변경하여 import한 `checkRateLimit`과 충돌 방지. 호출부 (149행 부근) `checkRateLimit(ip)` -> `checkLoginRateLimit(ip)` 수정.
- `recordFailure(ip)` 함수에서 인메모리 폴백 제거:
```typescript
async function recordFailure(ip: string): Promise<void> {
  if (!redis) return;
  try {
    const countKey = `cp:login:attempts:${ip}`;
    const lockKey = `cp:login:lock:${ip}`;
    const count = await redis.incr(countKey);
    await redis.expire(countKey, LOCK_DURATION_S);
    if (count >= MAX_ATTEMPTS) {
      await redis.set(lockKey, 1, { ex: LOCK_DURATION_S });
    }
  } catch { /* Redis 실패 시 무시 — 가용성 우선 */ }
}
```
- `clearAttempts(ip)` 함수에서 인메모리 폴백 제거:
```typescript
async function clearAttempts(ip: string): Promise<void> {
  if (!redis) return;
  try {
    await redis.del(`cp:login:attempts:${ip}`, `cp:login:lock:${ip}`);
  } catch { /* Redis 실패 시 무시 */ }
}
```
- `import { Redis } from "@upstash/redis";` 제거 (더 이상 직접 사용 안 함)
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && grep -r "commentRateMap\|cronRateLimitMap\|memAttempts\|MEM_MAX_SIZE\|COMMENT_WINDOW_MS\|getMemEntry\|evictExpired" src/ | grep -v node_modules | wc -l && pnpm build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "commentRateMap" src/` 결과 0건
    - `grep -r "cronRateLimitMap" src/` 결과 0건
    - `grep -r "memAttempts" src/` 결과 0건
    - `grep -r "MEM_MAX_SIZE" src/` 결과 0건
    - `grep -r "evictExpired" src/` 결과 0건
    - `grep "redisCheckRateLimit" src/middleware.ts` 결과 1건 이상
    - `grep "redisCheckRateLimit" src/app/api/db/comments/route.ts` 결과 1건 이상
    - `grep 'import.*redis.*from.*@/lib/redis' src/app/api/auth/login/route.ts` 결과 1건
    - `grep 'import { Redis }' src/app/api/auth/login/route.ts` 결과 0건
    - `pnpm build` 성공
  </acceptance_criteria>
  <done>인메모리 rate limit 코드 3곳이 완전히 제거되고 Redis 전용(또는 Redis 없으면 허용)으로 전환, 빌드 성공</done>
</task>

<task type="auto">
  <name>Task 2: Cookie secure 하드코딩 + temp 파일 정리 (SEC-02, CLEAN-01)</name>
  <files>src/app/api/auth/login/route.ts, .gitignore</files>
  <read_first>
    - src/app/api/auth/login/route.ts (Task 1 적용 후 상태 — secure: process.env.NODE_ENV 3곳 확인)
    - .gitignore (기존 temp 패턴 확인)
  </read_first>
  <action>
**SEC-02: Cookie secure 플래그 하드코딩**

src/app/api/auth/login/route.ts에서 3곳의 `secure: process.env.NODE_ENV === "production"` 을 `secure: true`로 교체:

1. POST 핸들러 내 비상 관리자 로그인 (204행 부근): `secure: process.env.NODE_ENV === "production"` → `secure: true`
2. POST 핸들러 내 일반 로그인 (253행 부근): `secure: process.env.NODE_ENV === "production"` → `secure: true`
3. DELETE 핸들러 내 로그아웃 (273행 부근): `secure: process.env.NODE_ENV === "production"` → `secure: true`

**CLEAN-01: Temp 파일 삭제 + git 추적 해제**

```bash
cd D:/Users/Documents/monet-registry-main
# git에서 추적 해제 (파일은 유지하지 않고 삭제)
git rm -f temp_ai_output.html temp_articles_output.html temp_dashboard_output.html temp_press_output.html tmp_pma.html cookies.txt nul 2>/dev/null || true
# .gitignore는 이미 패턴 존재 (79-84행: temp_*.html, temp_*.txt, tmp_*.html, nul, cookies.txt) — 추가 불필요
```

파일 7개: temp_ai_output.html, temp_articles_output.html, temp_dashboard_output.html, temp_press_output.html, tmp_pma.html, cookies.txt, nul
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && grep -c "process.env.NODE_ENV" src/app/api/auth/login/route.ts && echo "---" && ls temp_*.html tmp_*.html cookies.txt nul 2>/dev/null | wc -l && echo "---" && grep -c "secure: true" src/app/api/auth/login/route.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "secure: process.env" src/app/api/auth/login/route.ts` 결과 0건
    - `grep -c "secure: true" src/app/api/auth/login/route.ts` 결과 3건
    - `ls temp_*.html tmp_*.html cookies.txt nul 2>/dev/null` 결과 파일 없음
    - `git status` 에서 temp/tmp/cookies/nul 파일이 deleted 상태
    - `pnpm build` 성공
  </acceptance_criteria>
  <done>Cookie secure: true로 3곳 하드코딩, temp 파일 7개 삭제 및 git 추적 해제, 빌드 성공</done>
</task>

</tasks>

<verification>
- `grep -r "commentRateMap\|cronRateLimitMap\|memAttempts" src/` 결과 0건
- `grep "secure: process.env" src/app/api/auth/login/route.ts` 결과 0건
- `ls temp_*.html tmp_*.html cookies.txt nul 2>/dev/null` 결과 0건
- `pnpm build` 성공
</verification>

<success_criteria>
1. 인메모리 rate limit 코드 완전 제거 — Redis만 사용 (Redis 없으면 허용)
2. Cookie secure: true 하드코딩 — 환경 분기 제거
3. Temp 파일 삭제 + git 추적 해제 — 깨끗한 루트 디렉토리
</success_criteria>

<output>
After completion, create `.planning/phases/10-operational-stability/10-02-SUMMARY.md`
</output>
