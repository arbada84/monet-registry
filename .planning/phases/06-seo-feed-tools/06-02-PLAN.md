---
phase: 06-seo-feed-tools
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/upload/image/route.ts
  - src/app/api/upload/zip-articles/route.ts
  - src/app/api/ai/route.ts
  - src/app/api/coupang/products/route.ts
  - src/app/api/v1/articles/[id]/route.ts
autonomous: true
requirements: [FED-04, TOL-01, TOL-02, TOL-03, TOL-04]

must_haves:
  truths:
    - "이미지 업로드 API가 인증 없는 요청을 401로 거부한다"
    - "ZIP 업로드 API가 인증 없는 요청을 401로 거부한다"
    - "AI API가 50,000자 초과 content를 거부한다"
    - "쿠팡 API가 인증 없는 요청을 401로 거부한다"
    - "API v1 PUT에서 '상신' 상태가 유효하다"
  artifacts:
    - path: "src/app/api/upload/image/route.ts"
      provides: "인증된 이미지 업로드"
      contains: "verifyAuthToken"
    - path: "src/app/api/upload/zip-articles/route.ts"
      provides: "인증된 ZIP 업로드"
      contains: "verifyAuthToken"
    - path: "src/app/api/ai/route.ts"
      provides: "입력 길이 제한된 AI API"
      contains: "50000"
    - path: "src/app/api/coupang/products/route.ts"
      provides: "인증된 쿠팡 API"
      contains: "verifyAuthToken"
    - path: "src/app/api/v1/articles/[id]/route.ts"
      provides: "상신 상태 포함 PUT"
      contains: "상신"
  key_links:
    - from: "src/app/api/upload/image/route.ts"
      to: "src/lib/cookie-auth.ts"
      via: "verifyAuthToken import"
      pattern: "verifyAuthToken"
    - from: "src/app/api/v1/articles/[id]/route.ts"
      to: "Article status workflow"
      via: "VALID array"
      pattern: "상신"
---

<objective>
CRITICAL 보안 버그 2건(이미지/ZIP 업로드 무인증)과 API 수정 3건을 처리한다.

Purpose: 무인증 업로드로 인한 스토리지 남용/무단 기사 등록 차단 + API 정상 동작 보장
Output: 인증 적용된 업로드 API, 길이 제한된 AI API, 상신 상태 지원 API v1
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-seo-feed-tools/06-RESEARCH.md

@src/app/api/upload/image/route.ts
@src/app/api/upload/zip-articles/route.ts
@src/app/api/ai/route.ts
@src/app/api/coupang/products/route.ts
@src/app/api/v1/articles/[id]/route.ts
@src/lib/cookie-auth.ts

<interfaces>
<!-- 인증에 사용할 기존 함수 -->
From src/lib/cookie-auth.ts:
```typescript
export async function verifyAuthToken(token: string): Promise<{ valid: boolean; email?: string; role?: string }>;
```

From src/lib/api-key.ts:
```typescript
export async function verifyApiKey(authHeader: string | null): Promise<boolean>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 이미지/ZIP 업로드 + 쿠팡 API 인증 추가 (BUG-08, BUG-09, BUG-12)</name>
  <files>src/app/api/upload/image/route.ts, src/app/api/upload/zip-articles/route.ts, src/app/api/coupang/products/route.ts</files>
  <read_first>src/app/api/upload/image/route.ts, src/app/api/upload/zip-articles/route.ts, src/app/api/coupang/products/route.ts, src/lib/cookie-auth.ts</read_first>
  <action>
3개 API에 인증을 추가한다. 모든 호출은 `/cam/*` 어드민 페이지에서 쿠키와 함께 발생하므로 cookie-auth 사용.

**BUG-08: 이미지 업로드 인증 (CRITICAL)**

`src/app/api/upload/image/route.ts`의 POST 함수 최상단에 인증 체크 추가:
```typescript
import { verifyAuthToken } from "@/lib/cookie-auth";

export async function POST(request: NextRequest) {
  // 관리자 인증 필수
  const cookie = request.cookies.get("cp-admin-auth");
  const auth = await verifyAuthToken(cookie?.value ?? "");
  if (!auth.valid) {
    return NextResponse.json({ success: false, error: "인증이 필요합니다." }, { status: 401 });
  }
  // ... 기존 로직 그대로 유지
}
```

**BUG-09: ZIP 업로드 인증 (CRITICAL)**

`src/app/api/upload/zip-articles/route.ts`의 POST 함수 최상단에 동일한 인증 체크 추가:
```typescript
import { verifyAuthToken } from "@/lib/cookie-auth";

export async function POST(req: NextRequest) {
  const cookie = req.cookies.get("cp-admin-auth");
  const auth = await verifyAuthToken(cookie?.value ?? "");
  if (!auth.valid) {
    return NextResponse.json({ success: false, error: "인증이 필요합니다." }, { status: 401 });
  }
  // ... 기존 로직 그대로 유지
}
```

**BUG-12: 쿠팡 API 인증**

`src/app/api/coupang/products/route.ts`의 GET 함수 최상단에 인증 체크 추가:
```typescript
import { verifyAuthToken } from "@/lib/cookie-auth";

export async function GET(request: NextRequest) {
  const cookie = request.cookies.get("cp-admin-auth");
  const auth = await verifyAuthToken(cookie?.value ?? "");
  if (!auth.valid) {
    return NextResponse.json({ success: false, error: "인증이 필요합니다." }, { status: 401 });
  }
  // ... 기존 로직 그대로 유지
}
```

주의: import 추가 시 기존 import 순서 유지. 기존 코드는 인증 체크 아래로 들여쓰기 변경 없이 그대로 둔다 (try-catch 안에 있으면 그 밖에 인증을 배치).
  </action>
  <verify>
    <automated>cd "D:/Users/Documents/monet-registry-main" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - upload/image POST에 verifyAuthToken 호출 존재, 실패 시 401 반환
    - upload/zip-articles POST에 verifyAuthToken 호출 존재, 실패 시 401 반환
    - coupang/products GET에 verifyAuthToken 호출 존재, 실패 시 401 반환
    - 빌드 성공
  </acceptance_criteria>
  <done>3개 API가 인증 없는 요청을 401로 거부한다</done>
</task>

<task type="auto">
  <name>Task 2: AI content 길이 제한 + API v1 PUT 상신 상태 추가 (BUG-10, BUG-17)</name>
  <files>src/app/api/ai/route.ts, src/app/api/v1/articles/[id]/route.ts</files>
  <read_first>src/app/api/ai/route.ts, src/app/api/v1/articles/[id]/route.ts</read_first>
  <action>
2건의 API 버그를 수정한다.

**BUG-10: AI content 길이 제한**

`src/app/api/ai/route.ts`의 POST 함수에서 content 유효성 검사 직후(64행 부근, `if (!resolvedKey || !prompt || !content)` 체크 후)에 길이 제한 추가:
```typescript
// content 길이 제한 (50,000자 = 약 토큰 12,500개)
if (typeof content === "string" && content.length > 50000) {
  return NextResponse.json(
    { success: false, error: "content가 너무 깁니다. 50,000자 이하로 줄여주세요." },
    { status: 400 },
  );
}
```

50,000자 기준: 한글 기사 기준 약 25,000단어, OpenAI/Gemini 토큰 약 12,500개. 일반 기사(2,000~5,000자)의 10배 여유.

**BUG-17: API v1 PUT 상신 상태 추가**

`src/app/api/v1/articles/[id]/route.ts` 64행의 VALID 배열 수정:
```typescript
// 현재
const VALID = ["게시", "임시저장", "예약"];
// 수정 — "상신" 추가 (POST 엔드포인트와 동일하게)
const VALID = ["게시", "임시저장", "예약", "상신"];
```

에러 메시지도 업데이트:
```typescript
return NextResponse.json({ success: false, error: "올바르지 않은 status 값입니다. (게시|임시저장|예약|상신)" }, { status: 400 });
```

BUG-11 (Gemini systemInstruction)은 MEDIUM이지만 Gemini SDK 변경이 필요하여 최소 변경 원칙에 따라 이번 범위에서 제외.
BUG-13, BUG-14는 LOW이며 기능상 문제 없으므로 제외.
BUG-15는 LOW이며 실제 문제 발생 빈도가 낮으므로 제외.
  </action>
  <verify>
    <automated>cd "D:/Users/Documents/monet-registry-main" && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - AI route에 content.length > 50000 체크 존재, 초과 시 400 반환
    - API v1 PUT의 VALID 배열에 "상신" 포함
    - PUT 에러 메시지에 "상신" 포함
    - 빌드 성공
  </acceptance_criteria>
  <done>AI API가 50,000자 초과 입력을 거부하고, API v1 PUT이 상신 상태를 허용한다</done>
</task>

</tasks>

<verification>
- `pnpm build` 성공
- `vercel deploy --prod` 성공
- grep으로 각 파일의 인증/검증 코드 존재 확인
</verification>

<success_criteria>
1. 이미지 업로드 API가 쿠키 인증을 요구한다
2. ZIP 업로드 API가 쿠키 인증을 요구한다
3. 쿠팡 API가 쿠키 인증을 요구한다
4. AI API가 50,000자 초과 content를 400으로 거부한다
5. API v1 PUT이 "상신" 상태를 유효값으로 인정한다
6. `pnpm build` 성공, `vercel deploy --prod` 완료
</success_criteria>

<output>
After completion, create `.planning/phases/06-seo-feed-tools/06-02-SUMMARY.md`
</output>
