---
phase: 01-auth-security
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/middleware.ts
  - src/app/api/db/comments/route.ts
  - src/app/api/newsletter/unsubscribe/route.ts
  - src/app/api/ai/route.ts
  - src/app/api/auth/hash/route.ts
autonomous: true
requirements: [SEC-03, SEC-04]

must_haves:
  truths:
    - "동일 IP에서 cron API를 분당 6회 이상 호출하면 429 응답이 반환된다 (콜드스타트 이후에도)"
    - "동일 IP에서 10분 내 댓글 6개 이상 작성하면 429 응답이 반환된다 (콜드스타트 이후에도)"
    - "동일 IP에서 뉴스레터 구독해제를 분당 11회 이상 호출하면 429가 반환된다"
    - "동일 IP에서 AI API를 분당 21회 이상 호출하면 429가 반환된다"
    - "동일 IP에서 해시 API를 분당 11회 이상 호출하면 429가 반환된다"
    - "reporter 역할 사용자가 /cam/settings 접근 시 /cam/articles로 리다이렉트된다"
  artifacts:
    - path: "src/middleware.ts"
      provides: "Redis 기반 cron Rate Limiting + RBAC"
      contains: "checkRateLimit"
    - path: "src/app/api/db/comments/route.ts"
      provides: "Redis 기반 댓글 Rate Limiting"
      contains: "checkRateLimit"
    - path: "src/app/api/newsletter/unsubscribe/route.ts"
      provides: "Redis 기반 구독해제 Rate Limiting"
      contains: "checkRateLimit"
    - path: "src/app/api/ai/route.ts"
      provides: "Redis 기반 AI API Rate Limiting"
      contains: "checkRateLimit"
    - path: "src/app/api/auth/hash/route.ts"
      provides: "Redis 기반 해시 API Rate Limiting"
      contains: "checkRateLimit"
  key_links:
    - from: "src/middleware.ts"
      to: "src/lib/redis.ts"
      via: "import { checkRateLimit }"
      pattern: "import.*checkRateLimit.*from.*@/lib/redis"
    - from: "src/app/api/db/comments/route.ts"
      to: "src/lib/redis.ts"
      via: "import { checkRateLimit }"
      pattern: "import.*checkRateLimit.*from.*@/lib/redis"
---

<objective>
6개 인메모리 Rate Limiting 중 남은 5개(cron, 댓글, 뉴스레터, AI, 해시)를 모두 Plan 01에서 생성한 공통 Redis 유틸(src/lib/redis.ts)의 checkRateLimit으로 전환한다. RBAC은 기존 구현이 정상이므로 코드 리뷰로 검증한다.

Purpose: 서버리스 콜드스타트 후에도 모든 Rate Limiting이 작동하도록 한다 (SEC-03). RBAC 역할 제한이 올바른지 확인한다 (SEC-04).
Output: 5개 파일의 Rate Limiting이 Redis 기반으로 전환됨
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-security/01-RESEARCH.md
@.planning/phases/01-auth-security/01-01-SUMMARY.md

<interfaces>
<!-- Plan 01에서 생성된 공통 Redis 유틸 인터페이스 -->
From src/lib/redis.ts (Plan 01에서 생성):
```typescript
export { redis };  // InstanceType<typeof Redis> | null
export async function checkRateLimit(
  ip: string,
  prefix: string,
  maxPerWindow: number,
  windowSeconds: number
): Promise<boolean>;  // true=허용, false=제한초과
```

<!-- 전환 대상 인메모리 Rate Limiting 현황 (6개 중 1개는 login/route.ts에서 이미 Redis 사용) -->
| 파일 | 함수 | 제한 | Redis prefix | TTL |
|------|------|------|-------------|-----|
| middleware.ts | checkCronRateLimit | 분당 5회 | cp:cron:rate: | 60s |
| comments/route.ts | checkCommentRateLimit | 10분에 5개 | cp:comment:rate: | 600s |
| newsletter/unsubscribe/route.ts | isRateLimited | 분당 10회 | cp:newsletter:rate: | 60s |
| ai/route.ts | checkRateLimit | 분당 20회 | cp:ai:rate: | 60s |
| auth/hash/route.ts | checkRateLimit | 분당 10회 | cp:hash:rate: | 60s |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: middleware.ts cron Rate Limiting Redis 전환 + RBAC 검증</name>
  <files>src/middleware.ts</files>
  <read_first>
    - src/middleware.ts (전체 — checkCronRateLimit 구현과 3곳 호출부, RBAC 로직)
    - src/lib/redis.ts (Plan 01에서 생성된 checkRateLimit 유틸)
  </read_first>
  <action>
**1. import에 checkRateLimit 추가 (파일 상단):**
```typescript
import { checkRateLimit as redisCheckRateLimit } from "@/lib/redis";
```
(기존 import들과 함께 추가. 이름 충돌 방지를 위해 redisCheckRateLimit으로 alias.)

**2. checkCronRateLimit 함수를 async + Redis로 변경:**

기존 인메모리 Map(`cronRateLimitMap`)은 폴백용으로 유지하되, Redis를 우선 시도한다.

```typescript
async function checkCronRateLimit(ip: string): Promise<boolean> {
  // Redis 기반 Rate Limiting 시도 (src/lib/redis.ts 공통 유틸 사용)
  const result = await redisCheckRateLimit(ip, "cp:cron:rate:", 5, 60);
  if (result !== true) return result; // Redis가 동작하면 결과 사용
  // Redis 미설정 시 인메모리 폴백 (개발환경용)
  const now = Date.now();
  const entry = cronRateLimitMap.get(ip);
  if (!entry || now - entry.ts > 60000) {
    cronRateLimitMap.set(ip, { count: 1, ts: now });
    if (cronRateLimitMap.size > 1000) {
      for (const [key, val] of cronRateLimitMap) {
        if (now - val.ts > 120000) cronRateLimitMap.delete(key);
      }
    }
    return true;
  }
  if (entry.count >= 5) return false;
  entry.count++;
  return true;
}
```

**주의: checkRateLimit은 Redis 미설정 시에도 true를 반환하므로, Redis가 없으면 자동으로 인메모리 폴백 경로로 진행된다. 하지만 Redis가 설정되어 있고 요청이 허용된 경우에도 true를 반환하므로 구분이 안 됨.**

**대안 접근 (더 명확한 방식):** checkRateLimit의 반환값으로는 Redis 유무를 구분할 수 없으므로, 직접 redis를 import하여 null 체크:

```typescript
import { redis, checkRateLimit as redisCheckRateLimit } from "@/lib/redis";
```

```typescript
async function checkCronRateLimit(ip: string): Promise<boolean> {
  if (redis) {
    return redisCheckRateLimit(ip, "cp:cron:rate:", 5, 60);
  }
  // 인메모리 폴백 (개발환경용) — 기존 로직 유지
  const now = Date.now();
  const entry = cronRateLimitMap.get(ip);
  if (!entry || now - entry.ts > 60000) {
    cronRateLimitMap.set(ip, { count: 1, ts: now });
    if (cronRateLimitMap.size > 1000) {
      for (const [key, val] of cronRateLimitMap) {
        if (now - val.ts > 120000) cronRateLimitMap.delete(key);
      }
    }
    return true;
  }
  if (entry.count >= 5) return false;
  entry.count++;
  return true;
}
```

**3. checkCronRateLimit 호출부 3곳에 await 추가:**

각 호출부를 찾아서 `!checkCronRateLimit(...)` -> `!await checkCronRateLimit(...)` 변경:
- PUBLIC_GET_PATHS 비GET 요청 섹션 (약 line 96): `if (!await checkCronRateLimit(clientIp2))`
- /api/cron 경로 섹션 (약 line 123): `if (!await checkCronRateLimit(clientIp))`
- 내부 DB API 보호 섹션 (약 line 152): `if (!await checkCronRateLimit(clientIp))`

**CRITICAL: Promise는 truthy이므로 `!checkCronRateLimit(ip)`은 await 없이는 항상 false가 된다 (Rate Limiting 무효화). 3곳 모두 반드시 await.**

**4. RBAC 검증 (SEC-04):**
middleware.ts lines 176-183의 RBAC 로직을 검토:
- `REPORTER_ALLOWED_PATHS = ["/cam/login", "/cam/dashboard", "/cam/articles"]`
- reporter 역할이 허용 경로 외 /cam 접근 시 /cam/articles로 리다이렉트
- 이 로직이 정상이면 수정 불필요. 문제가 있으면 수정.

검증 항목:
- reporter가 `/cam/settings` 접근 -> `/cam/articles`로 리다이렉트 (OK)
- reporter가 `/cam/articles/123/edit` 접근 -> 허용 (`/cam/articles`로 startsWith 매칭) (OK)
- reporter가 `/cam` 접근 -> 허용 (pathname !== "/cam" 조건으로 허용) (OK)
- admin/superadmin은 모든 /cam 접근 허용 (role !== "reporter"이면 RBAC 블록 안 탐) (OK)
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - middleware.ts에 `import { redis, checkRateLimit as redisCheckRateLimit } from "@/lib/redis"` 존재
    - checkCronRateLimit이 async이고 `Promise<boolean>` 반환
    - Redis 사용 시 `cp:cron:rate:` prefix와 60초 윈도우
    - 3곳 호출부에서 모두 `await checkCronRateLimit` 패턴 존재
    - REPORTER_ALLOWED_PATHS가 ["/cam/login", "/cam/dashboard", "/cam/articles"] 유지
    - `npx tsc --noEmit` 에러 0건
  </acceptance_criteria>
  <done>
    - cron Rate Limiting이 Redis 기반으로 작동하며 서버리스 콜드스타트 후에도 분당 5회 제한 유지
    - RBAC이 reporter 역할을 올바르게 제한하고 있음을 확인
  </done>
</task>

<task type="auto">
  <name>Task 2: 4개 API 라우트 Rate Limiting Redis 전환 (댓글/뉴스레터/AI/해시)</name>
  <files>src/app/api/db/comments/route.ts, src/app/api/newsletter/unsubscribe/route.ts, src/app/api/ai/route.ts, src/app/api/auth/hash/route.ts</files>
  <read_first>
    - src/app/api/db/comments/route.ts (lines 61-93: commentRateMap 구현과 호출부)
    - src/app/api/newsletter/unsubscribe/route.ts (lines 13-35: rateLimitMap 구현과 호출부)
    - src/app/api/ai/route.ts (lines 9-28: rateLimitMap 구현과 호출부)
    - src/app/api/auth/hash/route.ts (lines 4-25: rateLimitMap 구현과 호출부)
    - src/lib/redis.ts (checkRateLimit 시그니처 확인)
  </read_first>
  <action>
4개 파일 모두 동일한 패턴으로 전환한다: 공통 `checkRateLimit`을 import하고, 기존 인메모리 로직을 Redis 우선 + 인메모리 폴백으로 변경.

**A. src/app/api/db/comments/route.ts:**

import 추가:
```typescript
import { redis, checkRateLimit as redisCheckRateLimit } from "@/lib/redis";
```

`checkCommentRateLimit`을 async로 변경:
```typescript
async function checkCommentRateLimit(ip: string): Promise<boolean> {
  if (redis) {
    const allowed = await redisCheckRateLimit(ip, "cp:comment:rate:", COMMENT_LIMIT, 600);
    if (!allowed) {
      console.warn(`[security] 댓글 Rate Limit 초과: ip=${ip.slice(0, 8)}***`);
    }
    return allowed;
  }
  // 인메모리 폴백 — 기존 로직 유지 (lines 68-92의 timestamps 기반 로직 그대로)
  const now = Date.now();
  const timestamps = (commentRateMap.get(ip) ?? []).filter((t) => now - t < COMMENT_WINDOW_MS);
  if (timestamps.length >= COMMENT_LIMIT) {
    console.warn(`[security] 댓글 Rate Limit 초과: ip=${ip.slice(0, 8)}***, count=${timestamps.length}`);
    return false;
  }
  timestamps.push(now);
  commentRateMap.set(ip, timestamps);
  if (now - lastRateCleanup > 120_000 || commentRateMap.size > 200) {
    lastRateCleanup = now;
    for (const [k, ts] of commentRateMap) {
      const fresh = ts.filter((t) => now - t < COMMENT_WINDOW_MS);
      if (fresh.length === 0) commentRateMap.delete(k);
      else commentRateMap.set(k, fresh);
    }
    if (commentRateMap.size > 500) {
      const entries = [...commentRateMap.entries()]
        .map(([k, ts]) => [k, Math.max(...ts)] as [string, number])
        .sort((a, b) => a[1] - b[1]);
      const toRemove = entries.slice(0, entries.length - 500);
      for (const [k] of toRemove) commentRateMap.delete(k);
    }
  }
  return true;
}
```

호출부(POST 핸들러 내)에서 `checkCommentRateLimit(ip)` -> `await checkCommentRateLimit(ip)` 변경. POST 핸들러가 이미 async이므로 await만 추가.

**B. src/app/api/newsletter/unsubscribe/route.ts:**

import 추가:
```typescript
import { redis, checkRateLimit as redisCheckRateLimit } from "@/lib/redis";
```

`isRateLimited`을 async로 변경:
```typescript
async function isRateLimited(ip: string): Promise<boolean> {
  if (redis) {
    const allowed = await redisCheckRateLimit(ip, "cp:newsletter:rate:", RATE_LIMIT_MAX, 60);
    return !allowed; // isRateLimited는 true=제한됨, checkRateLimit는 true=허용 (반전 주의!)
  }
  // 인메모리 폴백 — 기존 로직 유지
  const now = Date.now();
  if (++rlEvictCounter % 50 === 0) {
    for (const [k, v] of rateLimitMap) {
      if (now > v.resetAt) rateLimitMap.delete(k);
    }
  }
  const entry = rateLimitMap.get(ip);
  if (!entry || now > entry.resetAt) {
    rateLimitMap.set(ip, { count: 1, resetAt: now + RATE_LIMIT_WINDOW_MS });
    return false;
  }
  entry.count++;
  if (entry.count > RATE_LIMIT_MAX) return true;
  return false;
}
```

호출부(GET 핸들러 line 40): `if (isRateLimited(ip))` -> `if (await isRateLimited(ip))`. GET 핸들러가 이미 async.

**C. src/app/api/ai/route.ts:**

import 추가:
```typescript
import { redis, checkRateLimit as redisCheckRateLimit } from "@/lib/redis";
```

`checkRateLimit`을 async로 변경 (로컬 함수명이 import와 충돌하므로 주의):
```typescript
async function checkAiRateLimit(ip: string, maxPerMin = 20): Promise<boolean> {
  if (redis) {
    return redisCheckRateLimit(ip, "cp:ai:rate:", maxPerMin, 60);
  }
  // 인메모리 폴백 — 기존 로직 유지
  const now = Date.now();
  if (++rlEvictCounter % 100 === 0) {
    for (const [k, v] of rateLimitMap) {
      if (now - v.ts > 60000) rateLimitMap.delete(k);
    }
  }
  const entry = rateLimitMap.get(ip);
  if (!entry || now - entry.ts > 60000) {
    rateLimitMap.set(ip, { count: 1, ts: now });
    return true;
  }
  if (entry.count >= maxPerMin) return false;
  entry.count++;
  return true;
}
```

호출부(POST 핸들러 line 36): `if (!checkRateLimit(clientIp))` -> `if (!await checkAiRateLimit(clientIp))` (함수명 변경 반영).

**D. src/app/api/auth/hash/route.ts:**

import 추가:
```typescript
import { redis, checkRateLimit as redisCheckRateLimit } from "@/lib/redis";
```

`checkRateLimit`을 async로 변경:
```typescript
async function checkHashRateLimit(ip: string): Promise<boolean> {
  if (redis) {
    return redisCheckRateLimit(ip, "cp:hash:rate:", RATE_LIMIT, 60);
  }
  // 인메모리 폴백 — 기존 로직 유지
  const now = Date.now();
  if (rateLimitMap.size > 1000) {
    for (const [k, v] of rateLimitMap) {
      if (now > v.resetAt) rateLimitMap.delete(k);
    }
  }
  const entry = rateLimitMap.get(ip);
  if (!entry || now > entry.resetAt) {
    rateLimitMap.set(ip, { count: 1, resetAt: now + RATE_WINDOW });
    return true;
  }
  if (entry.count >= RATE_LIMIT) return false;
  entry.count++;
  return true;
}
```

호출부(POST 핸들러 line 37): `if (!checkRateLimit(ip))` -> `if (!await checkHashRateLimit(ip))` (함수명 변경 반영). POST 핸들러가 이미 async.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && npx tsc --noEmit 2>&1 | head -30 && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - 4개 파일 모두 `import { redis, checkRateLimit as redisCheckRateLimit } from "@/lib/redis"` 포함
    - comments/route.ts: `cp:comment:rate:` prefix, 600초 윈도우, async checkCommentRateLimit
    - newsletter/unsubscribe/route.ts: `cp:newsletter:rate:` prefix, 60초 윈도우, async isRateLimited
    - ai/route.ts: `cp:ai:rate:` prefix, 60초 윈도우, async checkAiRateLimit
    - auth/hash/route.ts: `cp:hash:rate:` prefix, 60초 윈도우, async checkHashRateLimit
    - 모든 호출부에 await 추가됨
    - `npx tsc --noEmit` 에러 0건
    - `pnpm build` 성공
  </acceptance_criteria>
  <done>
    - 6개 인메모리 Rate Limiting 위치 중 5개가 Redis로 전환됨 (login/route.ts는 기존에 이미 Redis 사용)
    - 모든 Rate Limiting이 서버리스 콜드스타트 후에도 작동
    - Redis 장애 시 인메모리 폴백으로 서비스 가용성 유지
    - 빌드 성공
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript 컴파일 에러 없음
2. `pnpm build` — Next.js 빌드 성공
3. grep 검증 (6개 Rate Limiting 모두 Redis 전환 확인):
   - `grep -rn "cp:cron:rate:" src/middleware.ts`
   - `grep -rn "cp:comment:rate:" src/app/api/db/comments/route.ts`
   - `grep -rn "cp:newsletter:rate:" src/app/api/newsletter/unsubscribe/route.ts`
   - `grep -rn "cp:ai:rate:" src/app/api/ai/route.ts`
   - `grep -rn "cp:hash:rate:" src/app/api/auth/hash/route.ts`
4. RBAC 검증: `grep -n "REPORTER_ALLOWED_PATHS" src/middleware.ts` — 역할 제한 로직 확인
5. 배포 후 수동 확인:
   - 동일 IP에서 cron API 6회 연속 호출 -> 429 반환
   - 동일 IP에서 댓글 6개 연속 작성 -> 429 반환
</verification>

<success_criteria>
- 6개 인메모리 Rate Limiting이 모두 Redis 기반으로 전환 (login은 기존 Redis, 나머지 5개 전환)
- 모든 Rate Limiting의 Redis 키 네임스페이스가 `cp:` 접두어로 체계적
- Redis 장애 시 인메모리 폴백으로 가용성 유지
- RBAC이 reporter 역할을 /cam/login, /cam/dashboard, /cam/articles로 올바르게 제한
- 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-security/01-02-SUMMARY.md`
</output>
