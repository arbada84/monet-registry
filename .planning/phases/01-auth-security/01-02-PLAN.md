---
phase: 01-auth-security
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/middleware.ts
  - src/app/api/db/comments/route.ts
autonomous: true
requirements: [SEC-03, SEC-04]

must_haves:
  truths:
    - "동일 IP에서 cron API를 분당 6회 이상 호출하면 429 응답이 반환된다 (서버리스 콜드스타트 이후에도)"
    - "동일 IP에서 10분 내 댓글 6개 이상 작성하면 429 응답이 반환된다 (서버리스 콜드스타트 이후에도)"
    - "reporter 역할 사용자가 /cam/settings 접근 시 /cam/articles로 리다이렉트된다"
  artifacts:
    - path: "src/middleware.ts"
      provides: "Redis 기반 cron Rate Limiting"
      contains: "upstash/redis"
    - path: "src/app/api/db/comments/route.ts"
      provides: "Redis 기반 댓글 Rate Limiting"
      contains: "upstash/redis"
  key_links:
    - from: "src/middleware.ts"
      to: "Upstash Redis"
      via: "@upstash/redis import"
      pattern: "Redis.*upstash"
    - from: "src/app/api/db/comments/route.ts"
      to: "Upstash Redis"
      via: "@upstash/redis import"
      pattern: "Redis.*upstash"
---

<objective>
미들웨어의 cron Rate Limiting과 댓글 Rate Limiting을 인메모리 Map에서 Upstash Redis로 전환하여, 서버리스 환경에서도 Rate Limiting이 실제로 작동하도록 수정한다.

Purpose: 현재 `cronRateLimitMap`(middleware.ts)과 `commentRateMap`(comments/route.ts)이 인메모리 Map으로 구현되어 Vercel 서버리스 콜드스타트마다 초기화됨. SEC-03(Rate Limiting) 요구사항을 충족하려면 Redis 전환이 필수. SEC-04(RBAC)는 기존 코드가 정상이므로 검증만 수행.
Output: Redis 기반 Rate Limiting이 적용된 middleware.ts와 comments/route.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
</context>

<interfaces>
<!-- 기존 middleware.ts Rate Limiting 인터페이스 -->

From src/middleware.ts:
```typescript
// cronRateLimitMap — in-memory Map<string, { count: number; ts: number }>
// checkCronRateLimit(ip: string): boolean — 분당 5회 제한
// middleware는 Edge Runtime에서 실행됨 — @upstash/redis는 Edge 호환
```

From src/app/api/db/comments/route.ts:
```typescript
// commentRateMap — in-memory Map<string, number[]>
// checkCommentRateLimit(ip: string): boolean — 10분에 5개 제한
// COMMENT_LIMIT = 5, COMMENT_WINDOW_MS = 10 * 60 * 1000
```

From src/app/api/auth/login/route.ts (Redis 초기화 참조 패턴):
```typescript
import { Redis } from "@upstash/redis";
let redis: InstanceType<typeof Redis> | null = null;
try {
  const url = process.env.UPSTASH_REDIS_REST_URL?.trim();
  const token = process.env.UPSTASH_REDIS_REST_TOKEN?.trim();
  if (url && token) { redis = new Redis({ url, token }); }
} catch (e) { console.error("[auth] Redis 초기화 실패:", e); }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: middleware.ts cron Rate Limiting을 Upstash Redis로 전환</name>
  <files>src/middleware.ts</files>
  <read_first>
    - src/middleware.ts
    - src/app/api/auth/login/route.ts (lines 5, 27-38 — Redis 초기화 패턴)
  </read_first>
  <action>
src/middleware.ts를 수정한다:

1. 파일 상단에 Upstash Redis import 및 초기화 추가:
```typescript
import { Redis } from "@upstash/redis";

let redis: InstanceType<typeof Redis> | null = null;
try {
  const url = process.env.UPSTASH_REDIS_REST_URL?.trim();
  const token = process.env.UPSTASH_REDIS_REST_TOKEN?.trim();
  if (url && token) {
    redis = new Redis({ url, token });
  }
} catch (e) {
  console.error("[middleware] Redis 초기화 실패:", e);
}
```

2. `checkCronRateLimit` 함수를 async로 변경하고 Redis 기반으로 구현:
```typescript
async function checkCronRateLimit(ip: string): Promise<boolean> {
  if (redis) {
    try {
      const key = `cp:cron:rate:${ip}`;
      const count = await redis.incr(key);
      if (count === 1) {
        await redis.expire(key, 60); // 1분 윈도우
      }
      return count <= 5;
    } catch (e) {
      console.error("[middleware] Redis Rate Limit 실패:", e);
      return true; // Redis 장애 시 통과 (가용성 우선)
    }
  }
  // 인메모리 폴백 (개발환경용)
  const now = Date.now();
  const entry = cronRateLimitMap.get(ip);
  if (!entry || now - entry.ts > 60000) {
    cronRateLimitMap.set(ip, { count: 1, ts: now });
    if (cronRateLimitMap.size > 1000) {
      for (const [key, val] of cronRateLimitMap) {
        if (now - val.ts > 120000) cronRateLimitMap.delete(key);
      }
    }
    return true;
  }
  if (entry.count >= 5) return false;
  entry.count++;
  return true;
}
```

3. `checkCronRateLimit` 호출부(3곳)에 모두 `await` 추가:
   - line ~96: `if (!await checkCronRateLimit(clientIp2))`
   - line ~123: `if (!await checkCronRateLimit(clientIp))`
   - line ~151: `if (!await checkCronRateLimit(clientIp))`

4. 인메모리 `cronRateLimitMap`은 개발환경 폴백용으로 유지. 주석에 "개발환경 전용 폴백"임을 명시.

주의: middleware.ts는 Edge Runtime에서 실행됨. @upstash/redis는 Edge Runtime 호환이므로 문제 없음.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && npx tsc --noEmit src/middleware.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/middleware.ts에 `import { Redis } from "@upstash/redis"` 존재
    - `checkCronRateLimit` 함수가 async이고 Redis 사용
    - Redis key 패턴 `cp:cron:rate:` 사용
    - TTL이 60초(1분 윈도우)
    - 3곳의 호출부에서 모두 `await checkCronRateLimit` 사용
    - Redis 실패 시 true 반환 (가용성 우선)
  </acceptance_criteria>
  <done>middleware.ts의 cron Rate Limiting이 Redis 기반으로 동작하며, 서버리스 콜드스타트 후에도 분당 5회 제한이 유지된다.</done>
</task>

<task type="auto">
  <name>Task 2: comments/route.ts 댓글 Rate Limiting을 Upstash Redis로 전환</name>
  <files>src/app/api/db/comments/route.ts</files>
  <read_first>
    - src/app/api/db/comments/route.ts (전체, 특히 lines 61-93 commentRateMap 부분)
    - src/app/api/auth/login/route.ts (lines 27-38 — Redis 초기화 패턴)
  </read_first>
  <action>
src/app/api/db/comments/route.ts를 수정한다:

1. Upstash Redis import 및 초기화 추가 (파일 상단, 기존 import 아래):
```typescript
import { Redis } from "@upstash/redis";

let redis: InstanceType<typeof Redis> | null = null;
try {
  const url = process.env.UPSTASH_REDIS_REST_URL?.trim();
  const token = process.env.UPSTASH_REDIS_REST_TOKEN?.trim();
  if (url && token) {
    redis = new Redis({ url, token });
  }
} catch (e) {
  console.error("[comments] Redis 초기화 실패:", e);
}
```

2. `checkCommentRateLimit` 함수를 async + Redis 기반으로 변경:
```typescript
async function checkCommentRateLimit(ip: string): Promise<boolean> {
  if (redis) {
    try {
      const key = `cp:comment:rate:${ip}`;
      const count = await redis.incr(key);
      if (count === 1) {
        await redis.expire(key, 600); // 10분 윈도우
      }
      if (count > COMMENT_LIMIT) {
        console.warn(`[security] 댓글 Rate Limit 초과: ip=${ip.slice(0, 8)}***, count=${count}`);
        return false;
      }
      return true;
    } catch (e) {
      console.error("[comments] Redis Rate Limit 실패:", e);
      return true; // Redis 장애 시 통과
    }
  }
  // 인메모리 폴백 (개발환경용)
  const now = Date.now();
  const timestamps = (commentRateMap.get(ip) ?? []).filter((t) => now - t < COMMENT_WINDOW_MS);
  if (timestamps.length >= COMMENT_LIMIT) {
    console.warn(`[security] 댓글 Rate Limit 초과: ip=${ip.slice(0, 8)}***, count=${timestamps.length}`);
    return false;
  }
  timestamps.push(now);
  commentRateMap.set(ip, timestamps);
  // 주기적 만료 정리 (기존 로직 유지)
  if (now - lastRateCleanup > 120_000 || commentRateMap.size > 200) {
    lastRateCleanup = now;
    for (const [k, ts] of commentRateMap) {
      const fresh = ts.filter((t) => now - t < COMMENT_WINDOW_MS);
      if (fresh.length === 0) commentRateMap.delete(k);
      else commentRateMap.set(k, fresh);
    }
  }
  return true;
}
```

3. `checkCommentRateLimit` 호출부에 `await` 추가 (POST 핸들러 내부에서 호출되는 곳을 찾아서):
```typescript
if (!await checkCommentRateLimit(ip)) {
  return NextResponse.json({ success: false, error: "..." }, { status: 429 });
}
```

4. 인메모리 `commentRateMap`과 관련 정리 로직은 개발환경 폴백용으로 유지.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && npx tsc --noEmit src/app/api/db/comments/route.ts 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/db/comments/route.ts에 `import { Redis } from "@upstash/redis"` 존재
    - `checkCommentRateLimit` 함수가 async이고 Redis 사용
    - Redis key 패턴 `cp:comment:rate:` 사용
    - TTL이 600초(10분 윈도우)
    - Redis 실패 시 true 반환 (가용성 우선)
    - 호출부에서 `await checkCommentRateLimit` 사용
  </acceptance_criteria>
  <done>댓글 Rate Limiting이 Redis 기반으로 동작하며, 서버리스 콜드스타트 후에도 10분 내 5개 제한이 유지된다.</done>
</task>

</tasks>

<verification>
1. 타입 검사: `npx tsc --noEmit` 에러 없음
2. 빌드 검사: `pnpm build` 성공
3. RBAC 검증 (SEC-04): middleware.ts의 REPORTER_ALLOWED_PATHS 로직이 정상인지 코드 리뷰로 확인
   - reporter 역할이 /cam/settings, /cam/users 등에 접근하면 /cam/articles로 리다이렉트
   - superadmin/admin은 모든 /cam/* 경로 접근 가능
4. 배포 후 확인:
   - cron API를 동일 IP에서 6회 이상 빠르게 호출 -> 429 반환 확인
   - 댓글을 동일 IP에서 6개 이상 빠르게 작성 시도 -> 429 반환 확인
</verification>

<success_criteria>
- cron Rate Limiting이 Redis 기반으로 동작하여 콜드스타트 후에도 분당 5회 제한 유지
- 댓글 Rate Limiting이 Redis 기반으로 동작하여 콜드스타트 후에도 10분당 5회 제한 유지
- RBAC이 reporter 역할을 허용 경로(/cam/login, /cam/dashboard, /cam/articles)로 제한
- pnpm build 성공
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-security/01-02-SUMMARY.md`
</output>
