---
phase: 01-auth-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/redis.ts
  - src/lib/cookie-auth.ts
  - src/app/api/auth/login/route.ts
  - src/middleware.ts
autonomous: true
requirements: [SEC-01, SEC-02, SEC-05]

must_haves:
  truths:
    - "로그아웃한 토큰이 Redis에 블랙리스트로 등록되어 서버리스 인스턴스 간 공유된다"
    - "블랙리스트에 등록된 토큰으로 어드민 페이지 접근 시 로그인 페이지로 리다이렉트된다"
    - "블랙리스트에 등록된 토큰으로 API 호출 시 401이 반환된다"
    - "Redis 장애 시 인메모리 폴백으로 서비스가 정상 작동한다"
  artifacts:
    - path: "src/lib/redis.ts"
      provides: "공통 Redis 인스턴스 + checkRateLimit 유틸"
      exports: ["redis", "checkRateLimit"]
    - path: "src/lib/cookie-auth.ts"
      provides: "async 토큰 블랙리스트 (Redis 우선, 인메모리 폴백)"
      contains: "async function invalidateToken"
    - path: "src/middleware.ts"
      provides: "getAuthState에서 블랙리스트 검사 포함"
      contains: "isTokenBlacklisted"
  key_links:
    - from: "src/lib/cookie-auth.ts"
      to: "src/lib/redis.ts"
      via: "import { redis }"
      pattern: "import.*redis.*from.*@/lib/redis"
    - from: "src/middleware.ts"
      to: "src/lib/cookie-auth.ts"
      via: "isTokenBlacklisted 호출"
      pattern: "await isTokenBlacklisted"
    - from: "src/app/api/auth/login/route.ts"
      to: "src/lib/cookie-auth.ts"
      via: "await invalidateToken"
      pattern: "await invalidateToken"
---

<objective>
Redis 공통 유틸을 생성하고, 토큰 블랙리스트를 인메모리 Set에서 Redis로 전환한다. middleware의 getAuthState에 블랙리스트 검사를 추가하여 로그아웃된 토큰의 어드민/API 접근을 차단한다.

Purpose: 서버리스 환경에서 로그아웃 토큰 무효화가 모든 인스턴스에 즉시 반영되도록 한다 (SEC-01, SEC-02, SEC-05)
Output: src/lib/redis.ts (공통 유틸), cookie-auth.ts (async 블랙리스트), middleware.ts (블랙리스트 검사 추가)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-security/01-RESEARCH.md

<interfaces>
<!-- 현재 cookie-auth.ts 공개 인터페이스 (변경 대상) -->
From src/lib/cookie-auth.ts:
```typescript
// 현재: 동기 함수 — async로 전환 필요
export function invalidateToken(token: string): void;
export function isTokenBlacklisted(token: string): boolean;

// 현재: isAuthenticated 내부에서 isTokenBlacklisted를 await 없이 호출 (동기이므로 OK였으나 async 전환 후 반드시 await 필요)
// line 161: if (tokenValue && isTokenBlacklisted(tokenValue)) return false;
export async function isAuthenticated(request: ...): Promise<boolean>;

export async function verifyAuthToken(value: string): Promise<TokenPayload>;
export async function getTokenPayload(request: ...): Promise<TokenPayload | null>;
export function timingSafeEqual(a: string, b: string): boolean;
export interface TokenPayload { valid: boolean; name: string; role: string; }
```

From src/middleware.ts:
```typescript
// getAuthState는 verifyAuthToken만 호출하고 isTokenBlacklisted를 검사하지 않음 — 보안 갭!
// 따라서 로그아웃 토큰으로 /cam/*, /api/db/* 접근이 가능한 상태
async function getAuthState(request: NextRequest): Promise<{ valid: boolean; role: string }>;
```

From src/app/api/auth/login/route.ts (line 268):
```typescript
// DELETE 핸들러에서 await 없이 호출 (현재 동기이므로 OK, async 전환 후 await 필수)
if (token) invalidateToken(token);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redis 공통 유틸 생성 + cookie-auth.ts 블랙리스트 async/Redis 전환</name>
  <files>src/lib/redis.ts, src/lib/cookie-auth.ts, src/app/api/auth/login/route.ts</files>
  <read_first>
    - src/lib/cookie-auth.ts (전체 — 블랙리스트 구현 + isAuthenticated 내 호출 확인)
    - src/app/api/auth/login/route.ts (lines 1-50: Redis 초기화 패턴, lines 260-279: DELETE 핸들러)
  </read_first>
  <action>
**1. src/lib/redis.ts 신규 생성 (공통 Redis 유틸):**

login/route.ts의 Redis 초기화 패턴(lines 27-38)을 공통 모듈로 추출한다. Edge Runtime + Node.js 모두 호환되어야 한다 (Node.js 전용 API 사용 금지).

```typescript
import { Redis } from "@upstash/redis";

let redis: InstanceType<typeof Redis> | null = null;
try {
  const url = process.env.UPSTASH_REDIS_REST_URL?.trim();
  const token = process.env.UPSTASH_REDIS_REST_TOKEN?.trim();
  if (url && token) {
    redis = new Redis({ url, token });
  }
} catch (e) {
  console.error("[redis] 초기화 실패:", e);
}

export { redis };

/**
 * Redis 기반 고정 윈도우 Rate Limiting (공통 유틸).
 * Redis 미설정 또는 장애 시 true 반환 (가용성 우선).
 * @returns true = 허용, false = 제한 초과
 */
export async function checkRateLimit(
  ip: string,
  prefix: string,
  maxPerWindow: number,
  windowSeconds: number
): Promise<boolean> {
  if (!redis) return true;
  try {
    const key = `${prefix}${ip}`;
    const count = await redis.incr(key);
    if (count === 1) {
      await redis.expire(key, windowSeconds);
    }
    return count <= maxPerWindow;
  } catch (e) {
    console.error(`[${prefix}] Redis Rate Limit 실패:`, e);
    return true;
  }
}
```

**2. src/lib/cookie-auth.ts 수정:**

파일 상단(기존 tokenBlacklist 선언 위)에 Redis import 추가:
```typescript
import { redis } from "@/lib/redis";
```

토큰 해시 헬퍼 함수 추가 (tokenBlacklist 아래, getSecret 위에 배치):
```typescript
/** 토큰의 SHA-256 해시 앞 16자를 Redis 키로 사용 (전체 토큰 저장 방지) */
async function getTokenHashKey(token: string): Promise<string> {
  const enc = new TextEncoder();
  const hash = await crypto.subtle.digest("SHA-256", enc.encode(token));
  const hex = Array.from(new Uint8Array(hash))
    .map((b) => b.toString(16).padStart(2, "0"))
    .join("");
  return `cp:blacklist:${hex.slice(0, 16)}`;
}
```

`invalidateToken` 함수를 async + Redis로 변경:
```typescript
export async function invalidateToken(token: string): Promise<void> {
  if (redis) {
    try {
      const key = await getTokenHashKey(token);
      await redis.set(key, "1", { ex: 86400 }); // 24h TTL (토큰 만료와 동일)
      return;
    } catch (e) {
      console.error("[cookie-auth] Redis 블랙리스트 저장 실패:", e);
    }
  }
  // 인메모리 폴백 (개발환경 / Redis 장애 시)
  tokenBlacklist.add(token);
}
```

`isTokenBlacklisted` 함수를 async + Redis로 변경:
```typescript
export async function isTokenBlacklisted(token: string): Promise<boolean> {
  if (redis) {
    try {
      const key = await getTokenHashKey(token);
      const exists = await redis.exists(key);
      return exists === 1;
    } catch (e) {
      console.error("[cookie-auth] Redis 블랙리스트 조회 실패:", e);
    }
  }
  return tokenBlacklist.has(token);
}
```

**CRITICAL: isAuthenticated 내 await 추가 (line 161):**
기존: `if (tokenValue && isTokenBlacklisted(tokenValue)) return false;`
변경: `if (tokenValue && await isTokenBlacklisted(tokenValue)) return false;`

주의: Promise 객체는 JavaScript에서 truthy이므로 await를 누락하면 `isTokenBlacklisted`가 항상 true로 평가되어 **모든 인증된 사용자가 차단**되는 치명적 버그 발생.

인메모리 tokenBlacklist Set과 setInterval 정리 로직은 폴백용으로 유지 (삭제 금지).

**3. src/app/api/auth/login/route.ts 수정 (line 268):**
기존: `if (token) invalidateToken(token);`
변경: `if (token) await invalidateToken(token);`
DELETE 핸들러가 이미 async이므로 await만 추가.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/redis.ts가 존재하고 `export { redis }` 및 `export async function checkRateLimit` 포함
    - cookie-auth.ts에 `import { redis } from "@/lib/redis"` 존재
    - cookie-auth.ts에 `async function invalidateToken(token: string): Promise<void>` 존재
    - cookie-auth.ts에 `async function isTokenBlacklisted(token: string): Promise<boolean>` 존재
    - cookie-auth.ts에 `cp:blacklist:` Redis 키 패턴 존재
    - cookie-auth.ts에 `{ ex: 86400 }` TTL 설정 존재
    - cookie-auth.ts isAuthenticated 내에서 `await isTokenBlacklisted` 패턴 존재
    - login/route.ts에 `await invalidateToken` 패턴 존재
    - `npx tsc --noEmit` 에러 0건
  </acceptance_criteria>
  <done>
    - redis.ts 공통 모듈이 Redis 인스턴스와 checkRateLimit 유틸을 export한다
    - 블랙리스트가 Redis SET + 24h TTL로 저장되며, Redis 장애 시 인메모리 폴백
    - 모든 async 호출부에 await가 추가되어 TypeScript 컴파일 통과
  </done>
</task>

<task type="auto">
  <name>Task 2: middleware.ts getAuthState에 블랙리스트 검사 추가</name>
  <files>src/middleware.ts</files>
  <read_first>
    - src/middleware.ts (전체 — getAuthState 함수 구현과 모든 호출 위치)
    - src/lib/cookie-auth.ts (Task 1 수정 후 — isTokenBlacklisted 새 시그니처)
  </read_first>
  <action>
**현재 보안 갭:** middleware.ts의 `getAuthState()`는 `verifyAuthToken()`만 호출하고 블랙리스트를 검사하지 않는다. 로그아웃된 토큰으로 /cam/* 어드민 페이지와 /api/db/* 등 보호 API에 접근 가능.

**수정 1: import에 isTokenBlacklisted 추가 (line 3):**
기존: `import { verifyAuthToken, timingSafeEqual } from "@/lib/cookie-auth";`
변경: `import { verifyAuthToken, timingSafeEqual, isTokenBlacklisted } from "@/lib/cookie-auth";`

**수정 2: getAuthState 함수에 블랙리스트 검사 추가 (lines 44-52):**
```typescript
async function getAuthState(request: NextRequest): Promise<{ valid: boolean; role: string }> {
  try {
    const cookie = request.cookies.get(ADMIN_COOKIE);
    const tokenValue = cookie?.value ?? "";
    if (!tokenValue) return { valid: false, role: "" };
    // 블랙리스트 검사 (로그아웃된 토큰 차단) — SEC-05
    if (await isTokenBlacklisted(tokenValue)) return { valid: false, role: "" };
    const result = await verifyAuthToken(tokenValue);
    return { valid: result.valid, role: result.valid ? ((result as { role?: string }).role || "admin") : "" };
  } catch {
    return { valid: false, role: "" };
  }
}
```

**영향 범위 확인:** getAuthState는 middleware 내에서 다음 위치에서 호출된다:
- line 101: PUBLIC_GET_PATHS 비GET 요청 인증
- line 128: /api/cron 경로 인증
- line 157: /api/db 등 내부 API 인증
- line 168: /cam/* 어드민 페이지 인증

모든 보호 경로에서 블랙리스트 검사가 자동 적용된다. 개별 호출부 수정 불필요.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && npx tsc --noEmit 2>&1 | head -20 && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - middleware.ts import문에 `isTokenBlacklisted` 포함
    - getAuthState 함수 내에 `await isTokenBlacklisted(tokenValue)` 존재
    - 블랙리스트 hit 시 `{ valid: false, role: "" }` 반환
    - `npx tsc --noEmit` 에러 0건
    - `pnpm build` 성공
  </acceptance_criteria>
  <done>
    - middleware가 모든 보호 경로에서 블랙리스트를 검사한다
    - 로그아웃된 토큰으로 /cam/* 접근 시 /cam/login으로 리다이렉트
    - 로그아웃된 토큰으로 /api/db/* 호출 시 401 반환
    - 빌드 성공
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript 컴파일 에러 없음
2. `pnpm build` — Next.js 빌드 성공 (Edge Runtime 호환성 포함)
3. grep 검증:
   - `grep -n "async function invalidateToken" src/lib/cookie-auth.ts` — async 전환
   - `grep -n "async function isTokenBlacklisted" src/lib/cookie-auth.ts` — async 전환
   - `grep -n "await isTokenBlacklisted" src/lib/cookie-auth.ts src/middleware.ts` — 호출부 await
   - `grep -n "await invalidateToken" src/app/api/auth/login/route.ts` — 호출부 await
   - `grep -n "cp:blacklist:" src/lib/cookie-auth.ts` — Redis 키 패턴
   - `grep -n "export.*redis" src/lib/redis.ts` — 공통 유틸 export
</verification>

<success_criteria>
- Redis에 토큰 블랙리스트가 SHA-256 해시 키로 저장된다
- middleware가 모든 보호 경로에서 블랙리스트를 검사한다
- Redis 장애 시 인메모리 폴백으로 정상 작동한다
- async 전환으로 인한 호출부 누락이 없다
- 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-security/01-01-SUMMARY.md`
</output>
