---
phase: 11-code-cleanup-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db-server.ts
  - src/app/api/auth/login/route.ts
  - src/app/api/db/newsletter/route.ts
  - src/lib/mysql-db.ts
  - src/lib/mysql.ts
  - src/lib/file-db.ts
autonomous: true
requirements: [CLEAN-02]

must_haves:
  truths:
    - "db-server.ts의 모든 함수가 Supabase 단일 경로로 동작한다"
    - "mysql-db, file-db, mysql 문자열이 src/ 디렉토리에서 import되지 않는다"
    - "pnpm build가 에러 없이 완료된다"
  artifacts:
    - path: "src/lib/db-server.ts"
      provides: "Supabase 단일 경로 thin wrapper"
      contains: "sbGetArticles"
    - path: "src/lib/mysql-db.ts"
      provides: "삭제됨"
      exists: false
    - path: "src/lib/mysql.ts"
      provides: "삭제됨"
      exists: false
    - path: "src/lib/file-db.ts"
      provides: "삭제됨"
      exists: false
  key_links:
    - from: "src/lib/db-server.ts"
      to: "src/lib/supabase-server-db.ts"
      via: "직접 import (dynamic import 제거)"
      pattern: "import.*supabase-server-db"
---

<objective>
MySQL/File DB 폴백 코드를 제거하고 db-server.ts를 Supabase 단일 경로 thin wrapper로 단순화한다.

Purpose: 프로덕션에서 사용하지 않는 데드 코드 561줄 중 ~360줄을 제거하여 유지보수성을 높인다.
Output: db-server.ts (~200줄), mysql-db.ts/mysql.ts/file-db.ts 삭제
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-code-cleanup-quality/11-RESEARCH.md
@src/lib/db-server.ts
@src/lib/supabase-server-db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: db-server.ts를 Supabase 단일 경로 thin wrapper로 리팩토링</name>
  <files>src/lib/db-server.ts</files>
  <read_first>
    - src/lib/db-server.ts (현재 561줄, 3단 폴백 패턴 확인)
    - src/lib/supabase-server-db.ts (위임 대상 함수명 확인)
  </read_first>
  <action>
db-server.ts를 리팩토링한다. 핵심 원칙: export 함수명과 타입 시그니처를 100% 유지, 내부 구현만 변경.

1. 파일 상단의 `isSupabaseEnabled()`, `isMySQLEnabled()` 헬퍼 함수 2개를 삭제한다.
2. mysql-db 및 file-db 관련 dynamic import를 모두 제거한다.
3. 각 함수를 supabase-server-db.ts의 대응 함수로 직접 위임하도록 변경한다.

변경 패턴 (모든 함수에 동일 적용):
```typescript
// 기존 (삭제)
export async function serverGetArticles(): Promise<Article[]> {
  if (isSupabaseEnabled()) {
    try {
      const { sbGetArticles } = await import("@/lib/supabase-server-db");
      return await sbGetArticles();
    } catch { /* ... */ }
  }
  if (isMySQLEnabled()) { ... }
  const { fileGetArticles } = await import("@/lib/file-db");
  return fileGetArticles();
}

// 변경 후
import { sbGetArticles } from "@/lib/supabase-server-db";
export async function serverGetArticles(): Promise<Article[]> {
  return sbGetArticles();
}
```

4. 파일 상단에 supabase-server-db.ts에서 필요한 모든 함수를 static import로 가져온다:
   - sbGetArticles, sbGetArticlesByCategory, sbGetArticlesByTag, sbSearchArticles
   - sbGetArticleById, sbGetArticleByNo, sbGetPublishedArticles, sbGetRecentArticles
   - sbGetArticleSitemapData, sbGetScheduledArticles, sbGetRecentTitles, sbGetTopArticles
   - sbGetFilteredArticles, sbGetSetting, sbSaveSetting
   - sbCreateArticle, sbUpdateArticle, sbDeleteArticle, sbPurgeArticle
   - sbGetDeletedArticles, sbIncrementViews
   - sbGetMaxArticleNo (serverGetNextArticleNo에서 사용)

5. serverCreateArticle 함수 내의 기사 번호 할당, parseTags, thumbnail 추출 등 비즈니스 로직은 그대로 유지한다. 이 함수는 단순 위임이 아닌 로직이 포함되어 있으므로 Supabase 호출 부분만 직접 import로 변경한다.

6. unstable_cache를 사용하는 serverGetSetting의 캐시 로직은 유지한다 (supabase-server-db.ts에는 캐시가 없으므로 db-server.ts의 캐시 wrapper가 필요).

7. serverGetViewLogs, serverAddViewLog, serverGetDistributeLogs, serverAddDistributeLogs, serverClearDistributeLogs도 동일하게 supabase-server-db.ts 함수로 위임한다. 대응하는 sb* 함수가 없는 경우 supabase-server-db.ts에서 직접 supabase 클라이언트를 import하여 구현한다.

8. 파일 상단 JSDoc 주석을 업데이트:
```typescript
/**
 * 서버 컴포넌트 전용 DB 접근 레이어
 * "use client" 파일에서는 import 불가 — 서버 컴포넌트/API 라우트 전용
 *
 * Supabase 단일 경로 (supabase-server-db.ts 위임)
 */
```

주의: cockroach-db.ts는 press-feed 기능의 활성 코드이므로 건드리지 않는다.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && grep -rn "mysql-db\|file-db\|isMySQLEnabled\|isSupabaseEnabled" src/lib/db-server.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - db-server.ts에서 `mysql-db` 문자열이 0건
    - db-server.ts에서 `file-db` 문자열이 0건
    - db-server.ts에서 `isMySQLEnabled` 문자열이 0건
    - db-server.ts에서 `isSupabaseEnabled` 문자열이 0건
    - db-server.ts에 `import.*supabase-server-db` 패턴이 존재
    - 모든 기존 export 함수명이 그대로 존재 (serverGetArticles, serverGetSetting 등 27개)
  </acceptance_criteria>
  <done>db-server.ts가 Supabase 단일 경로 thin wrapper로 변환되고, MySQL/File DB 폴백 코드가 완전 제거됨</done>
</task>

<task type="auto">
  <name>Task 2: login/newsletter 폴백 제거 + mysql-db.ts, mysql.ts, file-db.ts 파일 삭제</name>
  <files>
    src/app/api/auth/login/route.ts
    src/app/api/db/newsletter/route.ts
    src/lib/mysql-db.ts
    src/lib/mysql.ts
    src/lib/file-db.ts
  </files>
  <read_first>
    - src/app/api/auth/login/route.ts (115-126줄의 MySQL/file-db 폴백 확인)
    - src/app/api/db/newsletter/route.ts (81-92줄의 getDB 함수 확인)
    - src/lib/mysql-db.ts (삭제 대상 확인)
    - src/lib/mysql.ts (삭제 대상 확인)
    - src/lib/file-db.ts (삭제 대상 확인)
  </read_first>
  <action>
1. **login/route.ts** (103-126줄): 3단 폴백을 Supabase 단일 경로로 변경.

현재 코드 (삭제):
```typescript
if (accounts.length === 0 && process.env.NEXT_PUBLIC_SUPABASE_URL) {
  try {
    const { sbGetSetting, sbSaveSetting } = await import("@/lib/supabase-server-db");
    accounts = await sbGetSetting<Account[]>("cp-admin-accounts", [], true);
    saveAccountsFn = (data) => sbSaveSetting("cp-admin-accounts", data);
  } catch { /* 폴백 */ }
}
if (accounts.length === 0 && process.env.MYSQL_DATABASE) {
  try {
    const { dbGetSetting, dbSaveSetting } = await import("@/lib/mysql-db");
    accounts = await dbGetSetting<Account[]>("cp-admin-accounts", []);
    saveAccountsFn = (data) => dbSaveSetting("cp-admin-accounts", data);
  } catch { /* 폴백 */ }
}
if (accounts.length === 0) {
  const { fileGetSetting, fileSaveSetting } = await import("@/lib/file-db");
  accounts = fileGetSetting<Account[]>("cp-admin-accounts", []);
  saveAccountsFn = async (data) => fileSaveSetting("cp-admin-accounts", data);
}
```

변경 후:
```typescript
const { sbGetSetting, sbSaveSetting } = await import("@/lib/supabase-server-db");
accounts = await sbGetSetting<Account[]>("cp-admin-accounts", [], true);
saveAccountsFn = (data) => sbSaveSetting("cp-admin-accounts", data);
```

2. **newsletter/route.ts** (81-92줄): getDB() 함수를 Supabase 단일 경로로 단순화.

현재 코드 (삭제):
```typescript
async function getDB() {
  if (process.env.NEXT_PUBLIC_SUPABASE_URL && process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY) {
    const { sbGetSetting, sbSaveSetting } = await import("@/lib/supabase-server-db");
    return { dbGetSetting: sbGetSetting, dbSaveSetting: sbSaveSetting };
  }
  if (process.env.MYSQL_DATABASE) {
    const { dbGetSetting, dbSaveSetting } = await import("@/lib/mysql-db");
    return { dbGetSetting, dbSaveSetting };
  }
  const { fileGetSetting, fileSaveSetting } = await import("@/lib/file-db");
  return { dbGetSetting: fileGetSetting, dbSaveSetting: fileSaveSetting };
}
```

변경 후:
```typescript
async function getDB() {
  const { sbGetSetting, sbSaveSetting } = await import("@/lib/supabase-server-db");
  return { dbGetSetting: sbGetSetting, dbSaveSetting: sbSaveSetting };
}
```

3. **파일 삭제**: `git rm` 으로 아래 3개 파일을 삭제:
   - `src/lib/mysql-db.ts` (237줄)
   - `src/lib/mysql.ts` (23줄)
   - `src/lib/file-db.ts` (109줄)

4. **전체 codebase grep 검증**: `grep -rn "mysql-db\|file-db" src/` 결과가 0건인지 확인. 누락된 참조가 있으면 추가 수정.

5. **빌드 검증**: `pnpm build` 실행하여 TypeScript 컴파일 + 빌드 성공 확인.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && grep -rn "mysql-db\|file-db" src/ | wc -l && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "mysql-db" src/` 결과 0건
    - `grep -rn "file-db" src/` 결과 0건
    - src/lib/mysql-db.ts 파일이 존재하지 않음
    - src/lib/mysql.ts 파일이 존재하지 않음
    - src/lib/file-db.ts 파일이 존재하지 않음
    - login/route.ts에 `mysql-db` 또는 `file-db` 문자열 없음
    - newsletter/route.ts에 `mysql-db` 또는 `file-db` 문자열 없음
    - `pnpm build` 성공 (exit code 0)
  </acceptance_criteria>
  <done>MySQL/File DB 폴백이 login, newsletter 라우트에서 제거되고, mysql-db.ts, mysql.ts, file-db.ts가 삭제되며, 빌드가 성공함</done>
</task>

</tasks>

<verification>
1. `grep -rn "mysql-db\|file-db\|isMySQLEnabled" src/` — 결과 0건
2. `ls src/lib/mysql-db.ts src/lib/mysql.ts src/lib/file-db.ts 2>&1` — 모두 "No such file"
3. `pnpm build` — 성공
</verification>

<success_criteria>
- db-server.ts가 Supabase 단일 경로 thin wrapper로 동작
- MySQL/File DB 관련 코드가 src/ 전체에서 완전히 제거됨
- 프로덕션 빌드가 에러 없이 완료됨
</success_criteria>

<output>
After completion, create `.planning/phases/11-code-cleanup-quality/11-01-SUMMARY.md`
</output>
