---
phase: 09-press-import-auto-press-cockroachdb
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/app/api/cron/auto-press/route.ts
autonomous: true
requirements: [CDB-03, CDB-04]

must_haves:
  truths:
    - "auto-press가 뉴스와이어 소스에 대해 CockroachDB에서 미등록 건을 가져온다"
    - "auto-press가 뉴스와이어 외 소스(정부 보도자료)에 대해서는 기존 RSS 수집을 유지한다"
    - "기사 등록 후 press_feeds.registered=true + article_id가 업데이트된다"
    - "Vercel 환경변수 COCKROACH_DATABASE_URL이 등록되어 프로덕션에서 작동한다"
  artifacts:
    - path: "src/app/api/cron/auto-press/route.ts"
      provides: "CockroachDB 미등록 건 조회 + 등록 완료 표시 통합"
  key_links:
    - from: "src/app/api/cron/auto-press/route.ts"
      to: "src/lib/cockroach-db.ts"
      via: "import { getUnregisteredFeeds, markAsRegistered }"
      pattern: "getUnregisteredFeeds|markAsRegistered"
    - from: "src/app/api/cron/auto-press/route.ts"
      to: "serverCreateArticle"
      via: "기사 저장 후 markAsRegistered 호출"
      pattern: "markAsRegistered.*articleId"
---

<objective>
auto-press 크론 핸들러를 CockroachDB 통합으로 전환하고, Vercel 환경변수 등록 + 프로덕션 배포를 완료한다.

Purpose: auto-press가 RSS 실시간 파싱 대신 CockroachDB press_feeds에서 미등록 건을 가져오고, 등록 후 상태를 업데이트하여 중복 등록을 원천 차단한다.
Output: auto-press CockroachDB 연동 + Vercel 배포 완료
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-press-import-auto-press-cockroachdb/09-RESEARCH.md
@.planning/phases/09-press-import-auto-press-cockroachdb/09-01-SUMMARY.md
@src/lib/cockroach-db.ts
@src/app/api/cron/auto-press/route.ts

<interfaces>
<!-- Plan 01에서 생성한 cockroach-db.ts 핵심 인터페이스 -->
From src/lib/cockroach-db.ts:
```typescript
export interface PressFeed {
  id: string; source: string; source_no: number; title: string; url: string;
  date: string|null; category: string|null; company: string|null; summary: string|null;
  body_html: string|null; thumbnail: string|null; images: string[]; tags: string[];
  crawled_at: string; registered: boolean; article_id: string|null;
}

export async function getUnregisteredFeeds(options: {
  keywords?: string[]; dateFrom?: string; limit?: number;
}): Promise<PressFeed[]>;

export async function markAsRegistered(feedId: string, articleId: string): Promise<void>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: auto-press 뉴스와이어 소스를 CockroachDB 연동으로 전환</name>
  <files>src/app/api/cron/auto-press/route.ts</files>
  <read_first>
    - src/app/api/cron/auto-press/route.ts (전체 코드)
    - src/lib/cockroach-db.ts (Plan 01에서 생성)
    - .planning/phases/09-press-import-auto-press-cockroachdb/09-RESEARCH.md (교체 지점 분석)
  </read_first>
  <action>
**auto-press/route.ts 변경 내용:**

1. **import 추가:**
   ```typescript
   import { getUnregisteredFeeds, markAsRegistered, type PressFeed } from "@/lib/cockroach-db";
   ```

2. **runAutoPress 함수 내 소스 수집 로직 변경 (약 371~414줄):**

wrIds 미지정 시(일반 실행) 소스 수집 부분을 RSS + CockroachDB 하이브리드로 변경:

```typescript
// 뉴스와이어 소스: CockroachDB에서 미등록 건 조회
const newswireSources = activeSources.filter(s =>
  s.rssUrl?.includes("newswire.co.kr") || s.id?.includes("newswire")
);
const rssSources = activeSources.filter(s =>
  !s.rssUrl?.includes("newswire.co.kr") && !s.id?.includes("newswire") && s.rssUrl
);

// CockroachDB에서 뉴스와이어 미등록 건 가져오기
if (newswireSources.length > 0) {
  try {
    const dbFeeds = await getUnregisteredFeeds({
      keywords: keywords.length > 0 ? keywords : undefined,
      limit: count * 3,
    });
    for (const feed of dbFeeds) {
      const matchSource = newswireSources[0]; // 뉴스와이어 소스 매핑
      allItems.push({
        item: {
          id: feed.id, // UUID를 그대로 사용 (base64 변환 불필요)
          title: feed.title,
          date: feed.date || "",
          link: feed.url,
        },
        source: matchSource,
        _feedId: feed.id, // markAsRegistered용 원본 ID 보존
        _bodyHtml: feed.body_html, // 원문 fetch 건너뛰기용
        _images: feed.images,
        _thumbnail: feed.thumbnail,
      });
    }
  } catch (e) {
    console.error("[auto-press] CockroachDB 조회 실패, 뉴스와이어 건너뜀:", e);
  }
}

// 나머지 소스: 기존 RSS 수집 유지
const rssResults = await Promise.all(
  rssSources.map(async (source) => { ... })  // 기존 로직 그대로
);
```

3. **PressTarget 인터페이스 확장:**
   ```typescript
   interface PressTarget {
     item: RssTarget;
     source: AutoPressSource;
     _feedId?: string;      // CockroachDB press_feeds.id
     _bodyHtml?: string | null;  // DB에서 가져온 본문
     _images?: string[];
     _thumbnail?: string | null;
   }
   ```

4. **상세 수집 분기 (약 440~456줄):**
   CockroachDB에서 가져온 건은 body_html이 있으므로 fetchOriginContent 건너뛰기:
   ```typescript
   if (target._bodyHtml) {
     // CockroachDB에서 본문이 있으면 원문 fetch 건너뛰기
     const plainText = target._bodyHtml.replace(/<[^>]+>/g, "");
     detail = {
       title: item.title,
       bodyHtml: target._bodyHtml,
       bodyText: plainText,
       date: item.date,
       writer: "",
       images: target._images || [],
       sourceUrl: item.link,
     };
   } else if (item.link) {
     // 기존 원문 fetch 로직
     const origin = await fetchOriginContent(baseUrl, item.link);
     ...
   }
   ```

5. **기사 등록 후 markAsRegistered 호출 (약 623줄 serverCreateArticle 후):**
   ```typescript
   await serverCreateArticle(article);
   // CockroachDB 등록 완료 표시
   if (target._feedId) {
     try {
       await markAsRegistered(target._feedId, articleId);
     } catch (e) {
       console.warn("[auto-press] markAsRegistered 실패:", e);
       // 기사는 이미 저장됨 — 다음 실행 시 중복 체크로 방어
     }
   }
   ```

**주의사항:**
- 뉴스와이어 소스 판별: rssUrl에 "newswire.co.kr" 포함 여부로 판단
- 정부 보도자료 소스는 기존 RSS 로직 100% 유지
- markAsRegistered 실패해도 기사 저장에는 영향 없음 (try/catch 격리)
- 50초 타임아웃 로직은 변경하지 않음
- CockroachDB 조회 실패 시 뉴스와이어 건만 건너뛰고, 나머지 소스는 정상 처리
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - auto-press가 뉴스와이어 소스를 CockroachDB getUnregisteredFeeds()로 조회한다
    - 뉴스와이어 외 소스(정부 보도자료)는 기존 fetchRssFeed() 로직을 유지한다
    - 기사 등록 성공 시 markAsRegistered(feedId, articleId) 가 호출된다
    - CockroachDB 연결 실패 시에도 나머지 소스는 정상 처리된다
    - pnpm build 성공
  </acceptance_criteria>
  <done>auto-press가 CockroachDB 미등록 건 조회 + 등록 완료 표시를 수행하고 빌드가 성공한다</done>
</task>

<task type="auto">
  <name>Task 2: Vercel 환경변수 등록 + 프로덕션 배포</name>
  <files></files>
  <read_first>
    - .env.local (COCKROACH_DATABASE_URL 값 확인)
  </read_first>
  <action>
1. **Vercel 환경변수 등록:**
   ```bash
   # .env.local에서 COCKROACH_DATABASE_URL 값을 읽어 Vercel에 등록
   vercel env add COCKROACH_DATABASE_URL production
   ```
   - 또는 `vercel env ls`로 이미 등록되었는지 확인 후 미등록 시만 추가
   - Preview/Development 환경에도 동일하게 등록

2. **프로덕션 배포:**
   ```bash
   vercel deploy --prod
   ```

3. **배포 후 확인:**
   - 빌드 로그에 에러 없는지 확인
   - 배포 URL 정상 접근 확인

**주의:**
- COCKROACH_DATABASE_URL에 NEXT_PUBLIC_ 접두사 절대 붙이지 않음 (클라이언트 노출 방지)
- Vercel CLI가 인증 실패하면 checkpoint로 전환하여 수동 등록 안내
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && vercel ls 2>&1 | head -5</automated>
  </verify>
  <acceptance_criteria>
    - Vercel 환경변수에 COCKROACH_DATABASE_URL이 등록되어 있다
    - vercel deploy --prod가 성공하여 프로덕션에 배포되었다
  </acceptance_criteria>
  <done>Vercel 환경변수 등록 + 프로덕션 배포 완료</done>
</task>

</tasks>

<verification>
1. `pnpm build` 성공
2. auto-press가 뉴스와이어 소스를 DB에서 조회 + 등록 완료 표시
3. 정부 보도자료 소스는 기존 RSS 수집 유지
4. Vercel 프로덕션 배포 완료
5. 프로덕션에서 /api/press-feed?tab=newswire 정상 응답
</verification>

<success_criteria>
- auto-press 뉴스와이어 소스가 CockroachDB getUnregisteredFeeds()를 사용한다
- 기사 등록 후 markAsRegistered로 상태가 업데이트된다
- 정부 보도자료 소스는 기존 로직 유지
- Vercel COCKROACH_DATABASE_URL 등록 + 프로덕션 배포 성공
</success_criteria>

<output>
완료 후 `.planning/phases/09-press-import-auto-press-cockroachdb/09-02-SUMMARY.md` 생성
</output>
