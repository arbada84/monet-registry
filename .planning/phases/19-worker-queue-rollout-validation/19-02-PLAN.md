# Phase 19-02 Plan: Final v3.0 Linux CI closure

## Goal

Close `QA-01` and the v3.0 milestone by verifying the full Linux working tree after the auto-press operations, retry queue, Telegram, and Worker/Queue rollout work.

## Context

Phase 19-01 closed `AUTO-09` with focused Worker/Queue rollout tests and static guards. The remaining work is a final Linux-native validation pass that proves unit tests, route tests, Worker guards, planning guard, lint, typecheck, audit, metadata validation, and build all pass from `/home/arbada/dev/monet-registry-main`.

## Tasks

1. Verify Linux working tree baseline.
   - Confirm `node`, `pnpm`, git status, and diff hygiene.
   - Keep unrelated untracked files untouched.

2. Run final validation chain.
   - `pnpm check:auto-press-agent-loop`
   - `pnpm ci:all`
   - `node --check cloudflare/auto-press-worker/src/index.js`
   - `git diff --check`

3. Record closure.
   - Mark `QA-01` complete.
   - Mark Phase 19 and v3.0 complete.
   - Add 19-02 summary with commands and outcomes.

## Non-Goals

- Do not deploy to Vercel or Cloudflare.
- Do not push the local ahead commits.
- Do not modify unrelated untracked local documentation.

## Acceptance Criteria

- Final validation commands pass on Linux.
- Planning docs reflect `QA-01`, Phase 19, and v3.0 completion.
- A final closure commit records the verification.
