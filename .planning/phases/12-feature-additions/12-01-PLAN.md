---
phase: 12
plan: 01
title: "이미지 업로드 자동 리사이즈 + WebP 변환"
wave: 1
depends_on: []
requirements_addressed: [PERF-03]
files_modified:
  - src/app/api/upload/image/route.ts
  - src/app/cam/settings/page.tsx
  - src/lib/supabase-server-db.ts
autonomous: true
---

# Plan 12-01: 이미지 업로드 자동 리사이즈 + WebP 변환

<objective>
이미지 업로드 시 sharp를 사용하여 자동 리사이즈(최대 너비 제한) 및 WebP 변환을 적용하고, 어드민 설정에서 크기/품질을 조정할 수 있게 한다.
</objective>

<task id="12-01-01">
<title>이미지 설정 저장/조회 함수 추가 (supabase-server-db.ts)</title>
<read_first>
- src/lib/supabase-server-db.ts
- src/lib/watermark.ts
</read_first>
<action>
`src/lib/supabase-server-db.ts`에 이미지 업로드 설정 조회 함수를 추가한다.
기존 `getWatermarkSettings()`와 동일한 패턴으로 Supabase REST API를 직접 호출한다.

설정 키: `cp-image-settings`
인터페이스:
```typescript
export interface ImageUploadSettings {
  enabled: boolean;    // WebP 변환 활성화 여부 (기본: true)
  maxWidth: number;    // 최대 가로 크기 px (기본: 1920)
  quality: number;     // WebP 품질 1-100 (기본: 80)
}
```

기본값:
```typescript
const DEFAULT_IMAGE_SETTINGS: ImageUploadSettings = {
  enabled: true,
  maxWidth: 1920,
  quality: 80,
};
```

함수명: `getImageUploadSettings()` — watermark.ts의 `getWatermarkSettings()`와 동일 패턴.
`SUPABASE_URL`과 `SERVICE_KEY`로 직접 REST 호출, 실패 시 기본값 반환.

이 함수는 `src/app/api/upload/image/route.ts`에서 import하여 사용한다.
별도 파일이 아닌 supabase-server-db.ts에 추가하는 것이 기존 패턴과 일관된다.
단, watermark.ts가 `import "server-only"` 가드를 사용하는 것처럼, 이 함수도 서버 전용이다.
supabase-server-db.ts는 이미 서버 전용 파일이므로 추가 가드 불필요.
</action>
<acceptance_criteria>
- supabase-server-db.ts contains `export interface ImageUploadSettings`
- supabase-server-db.ts contains `export async function getImageUploadSettings()`
- supabase-server-db.ts contains `cp-image-settings`
- supabase-server-db.ts contains `maxWidth: 1920`
- supabase-server-db.ts contains `quality: 80`
</acceptance_criteria>
</task>

<task id="12-01-02">
<title>upload/image route.ts에 리사이즈 + WebP 변환 로직 삽입</title>
<read_first>
- src/app/api/upload/image/route.ts
- src/lib/watermark.ts
- src/lib/supabase-server-db.ts
</read_first>
<action>
`src/app/api/upload/image/route.ts`를 수정하여 이미지 리사이즈 및 WebP 변환을 추가한다.

1. 파일 상단에 import 추가:
```typescript
import sharp from "sharp";
import { getImageUploadSettings } from "@/lib/supabase-server-db";
```

2. POST 핸들러 내에서 `wmSettings` 로드 직후에 이미지 설정도 로드:
```typescript
let imgSettings: Awaited<ReturnType<typeof getImageUploadSettings>> | null = null;
try {
  imgSettings = await getImageUploadSettings();
} catch { /* 실패 시 변환 없이 진행 */ }
```

3. 새 함수 `maybeResizeAndConvert` 추가 (파일 내 `maybeApplyWatermark` 바로 위에):
```typescript
async function maybeResizeAndConvert(
  buf: ArrayBuffer,
  mime: string,
  settings: typeof imgSettings,
): Promise<{ buffer: ArrayBuffer; mime: string; ext: string }> {
  // 비활성화 또는 설정 없음 → 원본 유지
  if (!settings?.enabled) {
    return { buffer: buf, mime, ext: EXT_MAP[mime] ?? "jpg" };
  }
  // GIF는 변환하지 않음 (애니메이션 보존)
  if (mime === "image/gif") {
    return { buffer: buf, mime, ext: "gif" };
  }
  try {
    const processed = await sharp(Buffer.from(buf))
      .resize({ width: settings.maxWidth, withoutEnlargement: true })
      .webp({ quality: settings.quality })
      .toBuffer();
    return {
      buffer: new Uint8Array(processed).buffer as ArrayBuffer,
      mime: "image/webp",
      ext: "webp",
    };
  } catch {
    // sharp 실패 시 원본 유지
    return { buffer: buf, mime, ext: EXT_MAP[mime] ?? "jpg" };
  }
}
```

4. **multipart 업로드 경로** (line ~226 부근): 매직 바이트 검증 후, 워터마크 전에 리사이즈/변환 삽입:
기존:
```typescript
const ext = EXT_MAP[actualType] ?? "jpg";
buffer = await maybeApplyWatermark(buffer, actualType);
const resultUrl = await uploadToSupabase(buffer, actualType, ext);
```
변경:
```typescript
const resized = await maybeResizeAndConvert(buffer, actualType, imgSettings);
buffer = resized.buffer;
const finalMime = resized.mime;
const ext = resized.ext;
buffer = await maybeApplyWatermark(buffer, finalMime);
const resultUrl = await uploadToSupabase(buffer, finalMime, ext);
```

5. **URL 재호스팅 경로** (`fetchResolveUpload` 내): 직접 fetch 성공 후, 워터마크 전에 리사이즈/변환 삽입:
기존 (line ~183 부근):
```typescript
imgBuffer = await maybeApplyWatermark(imgBuffer, mimeType);
return uploadToSupabase(imgBuffer, mimeType, EXT_MAP[mimeType] ?? "jpg");
```
변경:
```typescript
const resized = await maybeResizeAndConvert(imgBuffer, mimeType, imgSettings);
imgBuffer = resized.buffer;
const finalMime = resized.mime;
const ext = resized.ext;
imgBuffer = await maybeApplyWatermark(imgBuffer, finalMime);
return uploadToSupabase(imgBuffer, finalMime, ext);
```

6. 프록시 폴백 경로도 동일하게 수정 (line ~201 부근):
기존:
```typescript
imgBuffer = await maybeApplyWatermark(imgBuffer, mimeType);
resultUrl = await uploadToSupabase(imgBuffer, mimeType, "jpg");
```
변경:
```typescript
const resizedProxy = await maybeResizeAndConvert(imgBuffer, mimeType, imgSettings);
imgBuffer = resizedProxy.buffer;
const finalMimeProxy = resizedProxy.mime;
const extProxy = resizedProxy.ext;
imgBuffer = await maybeApplyWatermark(imgBuffer, finalMimeProxy);
resultUrl = await uploadToSupabase(imgBuffer, finalMimeProxy, extProxy);
```

주의:
- `imgSettings`는 클로저로 `fetchResolveUpload` 내부에서 접근 가능해야 함
- `maybeResizeAndConvert`는 POST 핸들러 외부(모듈 레벨)에 정의
- `imgSettings`를 파라미터로 전달하는 패턴 사용
</action>
<acceptance_criteria>
- route.ts contains `import sharp from "sharp"`
- route.ts contains `import { getImageUploadSettings } from "@/lib/supabase-server-db"`
- route.ts contains `async function maybeResizeAndConvert(`
- route.ts contains `.webp({ quality: settings.quality })`
- route.ts contains `withoutEnlargement: true`
- route.ts contains `mime === "image/gif"` (GIF 제외 조건)
- route.ts contains `getImageUploadSettings()` 호출
</acceptance_criteria>
</task>

<task id="12-01-03">
<title>어드민 설정 페이지에 이미지 업로드 설정 섹션 추가</title>
<read_first>
- src/app/cam/settings/page.tsx
- src/lib/admin-styles.ts
- src/lib/db.ts
</read_first>
<action>
`src/app/cam/settings/page.tsx`에 이미지 업로드 설정 섹션을 추가한다.

1. 인터페이스 추가 (파일 상단, 기존 인터페이스들 근처):
```typescript
interface ImageUploadSettings {
  enabled: boolean;
  maxWidth: number;
  quality: number;
}

const DEFAULT_IMAGE_SETTINGS: ImageUploadSettings = {
  enabled: true,
  maxWidth: 1920,
  quality: 80,
};
```

2. 상태 추가 (기존 useState들 근처):
```typescript
const [imageSettings, setImageSettings] = useState<ImageUploadSettings>(DEFAULT_IMAGE_SETTINGS);
```

3. useEffect 데이터 로드에 추가 (기존 Promise.allSettled 배열에):
```typescript
getSetting<ImageUploadSettings>("cp-image-settings", DEFAULT_IMAGE_SETTINGS)
```
결과를 `setImageSettings`로 설정.

4. 저장 함수 (기존 `handleSave` 또는 별도):
기존 `handleSave`에 `cp-image-settings` 저장을 추가하거나, 별도 `handleSaveImageSettings` 함수 생성.
`saveSetting("cp-image-settings", imageSettings)` 호출.

5. UI 섹션 추가 (워터마크 설정 섹션 다음 위치):

```tsx
{/* 이미지 업로드 설정 */}
<div style={{ background: "#FFF", border: "1px solid #EEE", borderRadius: 8, padding: 16, marginBottom: 24 }}>
  <h3 style={{ fontSize: 16, fontWeight: 700, color: "#111", marginBottom: 16 }}>이미지 업로드 설정</h3>
  <div style={{ marginBottom: 12 }}>
    <label style={{ ...labelStyle, display: "flex", alignItems: "center", gap: 8 }}>
      <input
        type="checkbox"
        checked={imageSettings.enabled}
        onChange={(e) => setImageSettings({ ...imageSettings, enabled: e.target.checked })}
      />
      업로드 시 자동 WebP 변환
    </label>
  </div>
  <div style={{ marginBottom: 12 }}>
    <label style={labelStyle}>최대 가로 크기 (px)</label>
    <input
      type="number"
      style={inputStyle}
      value={imageSettings.maxWidth}
      min={100}
      max={4096}
      onChange={(e) => setImageSettings({ ...imageSettings, maxWidth: Number(e.target.value) || 1920 })}
    />
  </div>
  <div style={{ marginBottom: 12 }}>
    <label style={labelStyle}>WebP 변환 품질 (1-100)</label>
    <input
      type="number"
      style={inputStyle}
      value={imageSettings.quality}
      min={1}
      max={100}
      onChange={(e) => setImageSettings({ ...imageSettings, quality: Math.max(1, Math.min(100, Number(e.target.value) || 80)) })}
    />
  </div>
  <p style={{ fontSize: 12, color: "#999", marginTop: 8 }}>새로 업로드하는 이미지에만 적용됩니다. 기존 이미지는 변환되지 않습니다.</p>
</div>
```

기존 admin-styles.ts의 `inputStyle`과 `labelStyle`을 재사용한다.
</action>
<acceptance_criteria>
- settings/page.tsx contains `이미지 업로드 설정`
- settings/page.tsx contains `cp-image-settings`
- settings/page.tsx contains `업로드 시 자동 WebP 변환`
- settings/page.tsx contains `최대 가로 크기 (px)`
- settings/page.tsx contains `WebP 변환 품질 (1-100)`
- settings/page.tsx contains `새로 업로드하는 이미지에만 적용됩니다`
- settings/page.tsx contains `maxWidth: 1920`
</acceptance_criteria>
</task>

<verification>
## Verification
- [ ] `src/app/api/upload/image/route.ts` contains `sharp` import and `maybeResizeAndConvert` function
- [ ] `src/lib/supabase-server-db.ts` contains `getImageUploadSettings` export
- [ ] `src/app/cam/settings/page.tsx` contains `이미지 업로드 설정` section with all form fields
- [ ] GIF files are excluded from WebP conversion (`mime === "image/gif"`)
- [ ] Resize uses `withoutEnlargement: true` to prevent upscaling
- [ ] WebP conversion uses configurable quality from settings
- [ ] Settings page loads/saves `cp-image-settings` key
</verification>

<must_haves>
## Must-Haves (Goal-Backward)
- 이미지 업로드 시 자동으로 리사이즈 및 WebP 변환이 적용된다
- GIF는 변환에서 제외된다 (애니메이션 보존)
- 어드민 설정에서 최대 크기/품질 조정 가능
- sharp 실패 시 원본 이미지가 그대로 업로드된다 (graceful fallback)
- 기존 워터마크 기능이 정상 동작한다 (리사이즈 → WebP → 워터마크 순서)
</must_haves>
