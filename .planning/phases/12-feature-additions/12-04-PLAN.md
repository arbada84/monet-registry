---
phase: 12
plan: 04
title: "자동화 실행 이력 시각화 (Recharts 바 차트)"
wave: 2
depends_on: [12-01, 12-02]
requirements_addressed: [FEAT-01]
files_modified:
  - package.json
  - src/app/cam/dashboard/page.tsx
autonomous: true
---

# Plan 12-04: 자동화 실행 이력 시각화 (Recharts 바 차트)

<objective>
어드민 대시보드에 auto-press/auto-news 실행 이력을 Recharts 바 차트로 시각화하여 일별 성공/실패 건수를 한눈에 확인할 수 있게 한다.
</objective>

<task id="12-04-01">
<title>Recharts 패키지 설치</title>
<read_first>
- package.json
</read_first>
<action>
프로젝트에 recharts 패키지를 설치한다:
```bash
pnpm add recharts
```

설치 후 `package.json`의 dependencies에 `"recharts"` 항목이 추가됨을 확인한다.

**참고:** Recharts는 React 18+ 호환. 이 프로젝트는 React 19를 사용하므로 호환성 문제가 있을 수 있다. `--legacy-peer-deps` 또는 `pnpm add recharts --force` 필요할 수 있다. 설치 에러 시 `pnpm add recharts@latest` 시도.
</action>
<acceptance_criteria>
- package.json contains `"recharts"` in dependencies
- `node_modules/recharts` directory exists
</acceptance_criteria>
</task>

<task id="12-04-02">
<title>대시보드에 자동화 이력 차트 패널 추가</title>
<read_first>
- src/app/cam/dashboard/page.tsx
- src/types/article.ts (AutoPressRun, AutoNewsRun 타입)
- src/app/api/db/auto-press-settings/route.ts
- src/app/api/db/auto-news-settings/route.ts
</read_first>
<action>
`src/app/cam/dashboard/page.tsx`에 자동화 실행 이력 차트 패널을 추가한다.

1. Recharts import 추가 (파일 상단):
```typescript
import {
  BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, Legend, ResponsiveContainer,
} from "recharts";
```

2. 타입 정의 추가 (파일 상단, 기존 타입들 근처):
```typescript
interface AutoRunEntry {
  id: string;
  startedAt: string;
  completedAt: string;
  source: string;
  articlesPublished: number;
  articlesSkipped: number;
  articlesFailed: number;
}

interface ChartDataPoint {
  date: string;
  success: number;
  failure: number;
}
```

3. 상태 추가:
```typescript
const [pressHistory, setPressHistory] = useState<AutoRunEntry[]>([]);
const [newsHistory, setNewsHistory] = useState<AutoRunEntry[]>([]);
const [historyTab, setHistoryTab] = useState<"press" | "news">("press");
```

4. useEffect 데이터 로드에 추가 (Promise.allSettled 배열에 2개 추가):
```typescript
fetch("/api/db/auto-press-settings").then(r => r.json()).then(d => d.history || []),
fetch("/api/db/auto-news-settings").then(r => r.json()).then(d => d.history || []),
```
결과를 각각 `setPressHistory`, `setNewsHistory`로 설정.

5. 차트 데이터 변환 함수 (컴포넌트 내부):
```typescript
function toChartData(runs: AutoRunEntry[]): ChartDataPoint[] {
  const byDate: Record<string, { success: number; failure: number }> = {};
  for (const run of runs) {
    const date = run.startedAt?.slice(0, 10) || "unknown";
    if (!byDate[date]) byDate[date] = { success: 0, failure: 0 };
    byDate[date].success += run.articlesPublished || 0;
    byDate[date].failure += run.articlesFailed || 0;
  }
  return Object.entries(byDate)
    .sort(([a], [b]) => a.localeCompare(b))
    .slice(-10)
    .map(([date, counts]) => ({
      date: date.slice(5), // MM-DD 형식
      success: counts.success,
      failure: counts.failure,
    }));
}
```

6. 요약 통계 계산:
```typescript
const activeHistory = historyTab === "press" ? pressHistory : newsHistory;
const chartData = toChartData(activeHistory);
const recentRuns = activeHistory.slice(-10);
const totalSuccess = recentRuns.reduce((s, r) => s + (r.articlesPublished || 0), 0);
const totalFailure = recentRuns.reduce((s, r) => s + (r.articlesFailed || 0), 0);
```

7. UI 패널 (기존 대시보드 섹션들과 알림 패널 사이에 추가):
```tsx
{/* 자동화 실행 이력 */}
<div style={{ background: "#FFF", border: "1px solid #EEE", borderRadius: 8, padding: 16, marginBottom: 24 }}>
  <h3 style={{ fontSize: 16, fontWeight: 700, color: "#111", marginBottom: 16 }}>자동화 실행 이력</h3>
  {/* Tab bar */}
  <div style={{ display: "flex", gap: 0, borderBottom: "1px solid #EEE", marginBottom: 16 }}>
    <button
      onClick={() => setHistoryTab("press")}
      style={{
        padding: "8px 16px",
        cursor: "pointer",
        border: "none",
        background: "none",
        fontSize: 14,
        fontWeight: historyTab === "press" ? 700 : 400,
        color: historyTab === "press" ? "#E8192C" : "#999",
        borderBottom: historyTab === "press" ? "2px solid #E8192C" : "2px solid transparent",
      }}
    >
      보도자료 자동등록
    </button>
    <button
      onClick={() => setHistoryTab("news")}
      style={{
        padding: "8px 16px",
        cursor: "pointer",
        border: "none",
        background: "none",
        fontSize: 14,
        fontWeight: historyTab === "news" ? 700 : 400,
        color: historyTab === "news" ? "#E8192C" : "#999",
        borderBottom: historyTab === "news" ? "2px solid #E8192C" : "2px solid transparent",
      }}
    >
      자동 뉴스 발행
    </button>
  </div>
  {/* Chart or empty state */}
  {chartData.length === 0 ? (
    <div style={{ fontSize: 14, color: "#999", textAlign: "center", padding: "48px 0" }}>
      <p style={{ margin: 0, fontWeight: 700 }}>실행 이력이 없습니다</p>
      <p style={{ margin: "8px 0 0", fontSize: 13 }}>자동 수집이 실행되면 여기에 표시됩니다.</p>
    </div>
  ) : (
    <>
      <div style={{ width: "100%", height: 240 }}>
        <ResponsiveContainer width="100%" height="100%">
          <BarChart data={chartData}>
            <CartesianGrid strokeDasharray="3 3" />
            <XAxis dataKey="date" fontSize={12} />
            <YAxis fontSize={12} allowDecimals={false} />
            <Tooltip />
            <Legend />
            <Bar dataKey="success" name="성공" fill="hsl(12, 76%, 61%)" />
            <Bar dataKey="failure" name="실패" fill="hsl(173, 58%, 39%)" />
          </BarChart>
        </ResponsiveContainer>
      </div>
      <p style={{ fontSize: 12, color: "#666", marginTop: 12, textAlign: "center" }}>
        최근 {recentRuns.length}회: 성공 {totalSuccess}건 / 실패 {totalFailure}건
      </p>
    </>
  )}
</div>
```

색상:
- 성공 바: `hsl(12, 76%, 61%)` (chart-1)
- 실패 바: `hsl(173, 58%, 39%)` (chart-2)
차트 높이: 240px
X축: 날짜 (MM-DD), Y축: 건수
</action>
<acceptance_criteria>
- dashboard/page.tsx contains `import { BarChart, Bar, XAxis, YAxis` from recharts
- dashboard/page.tsx contains `자동화 실행 이력`
- dashboard/page.tsx contains `보도자료 자동등록` and `자동 뉴스 발행` tab labels
- dashboard/page.tsx contains `historyTab` state
- dashboard/page.tsx contains `toChartData` function
- dashboard/page.tsx contains `ResponsiveContainer`
- dashboard/page.tsx contains `실행 이력이 없습니다` empty state
- dashboard/page.tsx contains `hsl(12, 76%, 61%)` (chart-1 color)
- dashboard/page.tsx contains `최근` summary text
- dashboard/page.tsx contains `/api/db/auto-press-settings` and `/api/db/auto-news-settings`
</acceptance_criteria>
</task>

<verification>
## Verification
- [ ] recharts is listed in package.json dependencies
- [ ] Dashboard page imports Recharts components
- [ ] Two tabs ("보도자료 자동등록" / "자동 뉴스 발행") switch chart data
- [ ] Chart shows stacked bars with success (chart-1) and failure (chart-2) colors
- [ ] Empty state displays "실행 이력이 없습니다" with help text
- [ ] Summary row shows "최근 N회: 성공 X건 / 실패 Y건"
- [ ] Data is fetched from auto-press-settings and auto-news-settings APIs
- [ ] Chart height is 240px via ResponsiveContainer
</verification>

<must_haves>
## Must-Haves (Goal-Backward)
- 어드민 대시보드에서 auto-press/auto-news 실행 이력 차트를 확인할 수 있다
- 탭으로 보도자료/뉴스 이력을 전환할 수 있다
- 일별 성공/실패 건수가 바 차트로 표시된다
- 이력이 없을 때 빈 상태 메시지가 표시된다
- 최근 10회 요약 통계가 차트 아래에 표시된다
</must_haves>
