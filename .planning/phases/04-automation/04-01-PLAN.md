---
phase: 04-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/ai-prompt.ts
  - src/app/api/cron/auto-news/route.ts
  - src/app/api/cron/auto-press/route.ts
  - src/app/api/cron/publish/route.ts
autonomous: true
requirements: [AUT-01, AUT-04]

must_haves:
  truths:
    - "AI 편집 실패 시 5분 대기 없이 3회만 시도 후 즉시 반환된다"
    - "GET ?secret= 파라미터가 timing-safe 비교를 사용한다"
    - "auto-news DB 캐시가 TTL(30분)을 갖는다"
    - "status=fail인 기사가 중복으로 판정되지 않아 재시도 가능하다"
  artifacts:
    - path: "src/lib/ai-prompt.ts"
      provides: "AI 편집 함수 (5분 대기 제거)"
      contains: "tryOnce"
    - path: "src/app/api/cron/auto-news/route.ts"
      provides: "RSS cron (TTL 캐시, fail 재시도)"
      contains: "DB_CACHE_TTL"
    - path: "src/app/api/cron/auto-press/route.ts"
      provides: "보도자료 cron (timing-safe GET)"
      contains: "timingSafeEqual"
    - path: "src/app/api/cron/publish/route.ts"
      provides: "예약발행 cron (timing-safe GET)"
      contains: "timingSafeEqual"
  key_links:
    - from: "src/app/api/cron/auto-news/route.ts"
      to: "src/lib/ai-prompt.ts"
      via: "aiEditArticle() 호출"
      pattern: "aiEditArticle"
    - from: "src/app/api/cron/auto-news/route.ts"
      to: "isDuplicate()"
      via: "status=fail 제외 로직"
      pattern: "isDuplicate"
---

<objective>
자동화 파이프라인의 CRITICAL 버그(AI 5분 대기)와 핵심 버그(timing-safe, 캐시 TTL, 중복 판정) 6건을 수정한다.

Purpose: BUG-01(CRITICAL)이 전체 cron을 타임아웃시키고, BUG-10이 실패 기사 재시도를 막으며, BUG-02가 보안 취약점
Output: ai-prompt.ts, auto-news, auto-press, publish 4개 파일 수정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-automation/04-RESEARCH.md

<interfaces>
From src/lib/ai-prompt.ts (line 180-210):
- aiEditArticle() 함수의 재시도 루프: 1차 3회 (3초, 5초 대기) -> 5분 대기 -> 2차 2회
- 수정 대상: line 193-206 (5분 대기 + 2차 루프 전체 제거)

From src/lib/cookie-auth.ts:
- export function timingSafeEqual(a: string, b: string): boolean;
- 이미 auto-news와 auto-press에서 import되어 있음

From src/app/api/cron/auto-news/route.ts:
- line 15: import { verifyAuthToken, timingSafeEqual } from "@/lib/cookie-auth";
- line 271: let _dbArticlesCache (TTL 없음)
- line 287-299: isDuplicate() -- status=fail도 중복 판정
- line 670: GET secret === cronSecret (timing-safe 아님)

From src/app/api/cron/auto-press/route.ts:
- line 310-322: isDuplicate() -- status=fail도 중복 판정 (line 315)
- line 834: GET secret === cronSecret (timing-safe 아님)

From src/app/api/cron/publish/route.ts:
- line 108: GET secret === cronSecret (timing-safe 아님)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BUG-01 AI 5분 대기 제거 + BUG-02 GET secret timing-safe 교체</name>
  <files>src/lib/ai-prompt.ts, src/app/api/cron/auto-news/route.ts, src/app/api/cron/auto-press/route.ts, src/app/api/cron/publish/route.ts</files>
  <read_first>
    - src/lib/ai-prompt.ts (전체, 211줄)
    - src/app/api/cron/auto-news/route.ts (line 660-681, GET 함수)
    - src/app/api/cron/auto-press/route.ts (line 823-845, GET 함수)
    - src/app/api/cron/publish/route.ts (line 100-120, GET 함수)
  </read_first>
  <action>
**BUG-01 (CRITICAL) -- src/lib/ai-prompt.ts:**
- line 193-206 삭제: `// 2차: 5분 대기 후 2회 추가 시도` 블록 전체 제거
- line 193-194의 `console.warn`과 `await new Promise(r => setTimeout(r, 5 * 60 * 1000))` 제거
- line 197-206의 2차 for 루프 제거
- line 208의 에러 메시지를 `"(3회 시도 소진)"` 으로 변경 (5회 -> 3회)
- 결과: 1차 3회 시도 후 즉시 null 반환 (최대 ~15초 소요)

**BUG-02 (MEDIUM) -- 3개 파일의 GET 함수:**
- auto-news/route.ts line 670: `url.searchParams.get("secret") === cronSecret` -> `timingSafeEqual(url.searchParams.get("secret") ?? "", cronSecret)`
- auto-press/route.ts line 834: 동일하게 timingSafeEqual로 교체
- publish/route.ts line 108: 동일하게 timingSafeEqual로 교체. timingSafeEqual이 import되어 있지 않다면 import 추가: `import { timingSafeEqual } from "@/lib/cookie-auth";`
- 주의: timingSafeEqual은 두 인자 모두 string이어야 함. searchParams.get()이 null일 수 있으므로 `?? ""` 폴백 필수
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ai-prompt.ts에 5분 대기(300초 setTimeout) 코드가 없다
    - ai-prompt.ts에 2차 시도 루프가 없다
    - 3개 cron GET 핸들러 모두 timingSafeEqual을 사용한다
    - `===` 연산자로 secret을 비교하는 코드가 cron 폴더에 없다
  </acceptance_criteria>
  <done>AI 편집이 최대 3회만 시도하고, 모든 cron GET 인증이 timing-safe하다</done>
</task>

<task type="auto">
  <name>Task 2: BUG-04 캐시 TTL + BUG-10 fail 재시도 + BUG-11 normalizeTitle</name>
  <files>src/app/api/cron/auto-news/route.ts, src/app/api/cron/auto-press/route.ts</files>
  <read_first>
    - src/app/api/cron/auto-news/route.ts (line 265-310, 캐시+중복 함수)
    - src/app/api/cron/auto-press/route.ts (line 280-330, 캐시+중복 함수)
  </read_first>
  <action>
**BUG-04 (LOW) -- auto-news/route.ts:**
- line 271의 `_dbArticlesCache` 타입을 auto-press와 동일하게 `{ urls: Set<string>; titles: Set<string>; ts: number } | null`로 변경
- `getDbArticlesCache()` 함수에 TTL 체크 추가 (auto-press 패턴과 동일):
  ```
  const DB_CACHE_TTL = 30 * 60 * 1000; // 30분
  if (_dbArticlesCache && Date.now() - _dbArticlesCache.ts < DB_CACHE_TTL) return _dbArticlesCache;
  ```
- 캐시 저장 시 `ts: Date.now()` 추가

**BUG-10 (MEDIUM) -- auto-news/route.ts + auto-press/route.ts:**
- auto-news line 292: `(a.status === "ok" || a.status === "fail")` -> `a.status === "ok"` 로 변경
  - fail 상태 기사는 중복에서 제외하여 다음 cron에서 재시도 가능
- auto-press line 315: 동일하게 `(a.status === "ok" || a.status === "fail")` -> `a.status === "ok"` 로 변경

**BUG-11 (LOW) -- 두 파일의 normalizeTitle():**
- 현재: `t.replace(/\s+/g, "").replace(/[^\w가-힣]/g, "").toLowerCase().normalize("NFC")`
- 변경: `t.replace(/\s+/g, "").replace(/[^\p{L}\p{N}]/gu, "").toLowerCase().normalize("NFC")`
- `\p{L}` (유니코드 문자) + `\p{N}` (유니코드 숫자)로 다국어 지원
- `u` 플래그 필수 (유니코드 속성 이스케이프)
- 두 파일 모두 동일하게 수정
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - auto-news에 DB_CACHE_TTL 상수가 존재한다
    - auto-news와 auto-press의 isDuplicate에서 status=fail이 중복 조건에서 제외되었다
    - normalizeTitle이 유니코드 속성 이스케이프(\p{L}\p{N})를 사용한다
    - pnpm build 성공
  </acceptance_criteria>
  <done>캐시 TTL 30분 적용, fail 기사 재시도 가능, 다국어 제목 정규화 정상 작동</done>
</task>

</tasks>

<verification>
1. `pnpm build` 성공 (타입 에러 없음)
2. `grep -rn "5 \* 60 \* 1000" src/lib/ai-prompt.ts` -- 결과 없음 (5분 대기 제거 확인)
3. `grep -rn 'searchParams.get("secret") ===' src/app/api/cron/` -- 결과 없음 (timing-safe 교체 확인)
4. `grep -rn 'status === "fail"' src/app/api/cron/auto-news/ src/app/api/cron/auto-press/` -- 중복 판정에서 제거 확인
</verification>

<success_criteria>
- CRITICAL BUG-01 해결: AI 편집이 3회 시도 후 즉시 반환 (60초 타임아웃 내 안전)
- BUG-02 해결: 모든 cron GET 인증이 timing-safe
- BUG-04 해결: auto-news DB 캐시에 30분 TTL
- BUG-10 해결: fail 기사 재시도 가능
- BUG-11 해결: 다국어 제목 normalizeTitle 지원
</success_criteria>

<output>
After completion, create `.planning/phases/04-automation/04-01-SUMMARY.md`
</output>
