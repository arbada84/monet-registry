---
phase: 03-admin-cms
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/cam/articles/page.tsx
  - src/app/cam/dashboard/page.tsx
autonomous: true
requirements: [ADM-01, ADM-02, ADM-05]
must_haves:
  truths:
    - "기사 목록 페이지 진입 시 휴지통 버튼에 실제 삭제 기사 수가 표시된다"
    - "기사 복제 시 createdAt이 현재 시간으로 설정된다"
    - "대시보드에서 기사 본문(body)을 로드하지 않아 로딩이 빨라진다"
    - "유지보수 버튼이 기본 접힌 상태이고, 펼쳐야 보인다"
  artifacts:
    - path: "src/app/cam/articles/page.tsx"
      provides: "휴지통 카운트 + 복제 createdAt 수정"
      contains: "trashCount|createdAt"
    - path: "src/app/cam/dashboard/page.tsx"
      provides: "body 제외 로드 + 유지보수 버튼 접기"
      contains: "showMaintenance|limit"
  key_links:
    - from: "src/app/cam/articles/page.tsx"
      to: "/api/db/articles?trash=true"
      via: "휴지통 카운트 초기 로드"
      pattern: "trashCount|getDeletedArticles"
---

<objective>
기사 목록(articles/page.tsx)과 대시보드(dashboard/page.tsx)의 MEDIUM/HIGH 버그 4건 수정.

Purpose: 목록/대시보드 UX 개선 + 불필요한 데이터 로드 감소
Output: articles/page.tsx, dashboard/page.tsx 수정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-cms/03-RESEARCH.md

@src/app/cam/articles/page.tsx
@src/app/cam/dashboard/page.tsx
@src/lib/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: 휴지통 카운트 초기 로드 + 복제 createdAt (BUG-04/12)</name>
  <files>src/app/cam/articles/page.tsx</files>
  <read_first>
    - src/app/cam/articles/page.tsx (전체 — trashArticles 상태, loadTrash, handleDuplicate 확인)
    - src/lib/db.ts (getDeletedArticles 함수 시그니처)
  </read_first>
  <action>
BUG-04: 휴지통 카운트가 초기에 0으로 표시되는 문제 수정.
BUG-12: 기사 복제 시 createdAt이 누락되는 문제 수정.

수정 방법:

1. 휴지통 카운트 (BUG-04):
   - `const [trashCount, setTrashCount] = useState<number | null>(null)` 상태 추가
   - 페이지 초기 로드 useEffect (line ~57)의 Promise.allSettled에 `getDeletedArticles()` 호출 추가
   - 결과에서 `setTrashCount(deleted.length)` 설정 (전체 데이터를 trashArticles에 넣지 않고 카운트만)
   - 휴지통 버튼 표시 (line ~359)를 `trashArticles.length` 대신 `trashCount ?? ""` 사용:
     ```tsx
     휴지통{trashMode ? "" : trashCount !== null ? ` (${trashCount})` : ""}
     ```
   - loadTrash() 호출 후에도 `setTrashCount(data.length)` 갱신
   - 삭제/복원/영구삭제 시에도 trashCount 업데이트

2. 복제 createdAt (BUG-12):
   - handleDuplicate 함수 (line ~154) 에서 복제 기사 객체에 `createdAt: new Date().toISOString()` 추가
   - `date` 필드에도 현재 시간 설정: `date: new Date().toISOString()`

핵심: 초기 로드 시 휴지통 카운트를 별도 요청으로 가져와 버튼에 표시. 복제 시 시간 필드 누락 방지.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 기사 목록 진입 시 휴지통 버튼에 실제 삭제 기사 수 표시
    - 기사 삭제/복원 시 카운트 동기화
    - 복제된 기사에 createdAt, date 필드가 현재 시간으로 설정
    - 빌드 성공
  </acceptance_criteria>
  <done>휴지통 카운트가 초기부터 정확히 표시되고, 복제 기사의 시간 필드가 정상 설정됨</done>
</task>

<task type="auto">
  <name>Task 2: 대시보드 body 제외 로드 + 유지보수 버튼 접기 (BUG-05/11)</name>
  <files>src/app/cam/dashboard/page.tsx</files>
  <read_first>
    - src/app/cam/dashboard/page.tsx (전체 — getArticles 호출부, 유지보수 버튼 영역)
    - src/lib/db.ts (getArticles 파라미터 확인)
    - src/app/api/db/articles/route.ts (select 지원 여부)
  </read_first>
  <action>
BUG-05: 대시보드가 전체 기사(body 포함)를 로드하는 성능 문제 최소 수정.
BUG-11: 유지보수 버튼 과다 노출 문제.

수정 방법:

1. 대시보드 body 제외 (BUG-05 — 최소 수정, PERF-01은 v2):
   - 대시보드에서는 기사 본문(body)이 필요없음 (통계만 계산)
   - `getArticles()` 호출 (line ~38)에서 body를 가져온 후 즉시 제거하는 것은 의미 없음 (이미 전송됨)
   - 최소 수정: dashboard useEffect에서 `getArticles()` 대신 `getArticles({ limit: 10000 })` 호출 유지하되, 결과를 articles 상태에 저장할 때 body를 제거:
     ```tsx
     const arts = results[0].status === "fulfilled"
       ? results[0].value.map(({ body, ...rest }) => rest as Article)
       : [];
     ```
   - 이것은 클라이언트 메모리만 절약하는 최소 수정임. 서버사이드 select는 PERF-01(v2)에서 처리.

2. 유지보수 버튼 접기 (BUG-11):
   - `const [showMaintenance, setShowMaintenance] = useState(false)` 상태 추가
   - 유지보수 버튼 4개 (예약 발행, 번호 할당, 썸네일 중복, 이미지 재업로드 — lines 190-272)를 접기/펼치기 토글로 감싸기:
     ```tsx
     <div style={{ marginTop: 16 }}>
       <button onClick={() => setShowMaintenance(!showMaintenance)} style={{
         padding: "6px 12px", background: "transparent", border: "1px solid #DDD",
         borderRadius: 6, fontSize: 13, color: "#999", cursor: "pointer"
       }}>
         유지보수 도구 {showMaintenance ? "▲" : "▼"}
       </button>
       {showMaintenance && (
         <div style={{ display: "flex", gap: 8, flexWrap: "wrap", marginTop: 8 }}>
           {/* 기존 4개 버튼 그대로 */}
         </div>
       )}
     </div>
     ```

핵심: body 메모리 제거는 최소 변경. 유지보수 버튼은 기본 접힌 상태로 실수 방지.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 대시보드에서 articles 상태에 body가 포함되지 않음 (메모리 절약)
    - 유지보수 버튼이 기본 접힌 상태
    - "유지보수 도구" 토글 클릭 시 버튼 표시/숨김
    - 빌드 성공
  </acceptance_criteria>
  <done>대시보드 메모리 사용 감소 + 유지보수 버튼 실수 방지</done>
</task>

</tasks>

<verification>
- `pnpm build` 성공
- articles/page.tsx에서 trashCount 상태 및 초기 로드 확인
- dashboard/page.tsx에서 showMaintenance 상태 및 body 제거 확인
</verification>

<success_criteria>
- 휴지통 카운트 초기 표시 (BUG-04)
- 복제 기사 시간 필드 설정 (BUG-12)
- 대시보드 body 메모리 제거 (BUG-05 최소 수정)
- 유지보수 버튼 기본 접힌 상태 (BUG-11)
- 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-cms/03-02-SUMMARY.md`
</output>
