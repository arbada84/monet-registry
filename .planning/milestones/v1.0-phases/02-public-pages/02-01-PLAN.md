---
phase: 02-public-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/search/components/SearchContent.tsx
  - src/app/tag/[name]/page.tsx
autonomous: true
requirements: [PUB-04, PUB-05]

must_haves:
  truths:
    - "검색에서 최신순 정렬 후 페이지 이동 시 정렬 상태가 유지된다"
    - "태그 페이지에서 culturepeople 테마의 보라색(#5B4B9E) accent가 적용된다"
    - "태그 페이지에서 기사가 20건 이상이면 더보기 버튼이 나타난다"
  artifacts:
    - path: "src/app/search/components/SearchContent.tsx"
      provides: "검색 정렬 파라미터 유지 로직"
      contains: "sort !== \"\""
    - path: "src/app/tag/[name]/page.tsx"
      provides: "태그 페이지 accent 적용 + 페이지네이션"
      contains: "style={{ color: accent }}"
  key_links:
    - from: "SearchContent.tsx goToPage"
      to: "URL sort parameter"
      via: "URLSearchParams set"
      pattern: "sort !== \"\""
    - from: "tag/[name]/page.tsx accent"
      to: "하드코딩된 #E8192C 제거"
      via: "inline style binding"
      pattern: "style=.*accent"
---

<objective>
검색 페이지 정렬 파라미터 손실 버그와 태그 페이지 테마 색상/페이지네이션 문제를 수정한다.

Purpose: 검색 사용자가 정렬 변경 후 페이지 이동 시 정렬이 초기화되는 버그를 수정하고, 태그 페이지가 테마별 accent 색상을 적용하며 대량 기사에 페이지네이션을 제공한다.
Output: 수정된 SearchContent.tsx, tag/[name]/page.tsx
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-public-pages/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 검색 sort 파라미터 기본값 비교 수정 (BUG-08)</name>
  <files>src/app/search/components/SearchContent.tsx</files>
  <read_first>src/app/search/components/SearchContent.tsx</read_first>
  <action>
SearchContent.tsx에서 sort 기본값 비교 로직을 수정한다.

현재 버그: `initialSort !== "date"` 조건이 기본값(빈 문자열 = 관련도순)을 잘못 처리한다.
- 사용자가 "최신순"(date)을 선택하면, `"date" !== "date"` = false가 되어 sort 파라미터가 URL에서 제거됨
- 페이지 이동 시 정렬이 관련도순으로 초기화됨

수정: 3곳의 `sort !== "date"` 비교를 `sort !== ""`로 변경한다.

1. goToPage 함수 (약 라인 123):
   변경 전: `if (initialSort && initialSort !== "date") params.set("sort", initialSort);`
   변경 후: `if (initialSort && initialSort !== "") params.set("sort", initialSort);`
   → 단순화: `if (initialSort) params.set("sort", initialSort);` (빈 문자열은 falsy이므로)

2. handleCategoryChange 함수 (약 라인 146):
   변경 전: `if (initialSort && initialSort !== "date") params.set("sort", initialSort);`
   변경 후: `if (initialSort) params.set("sort", initialSort);`

3. handleSortChange 함수 (약 라인 155):
   변경 전: `if (sort && sort !== "date") params.set("sort", sort);`
   변경 후: `if (sort) params.set("sort", sort);`

이렇게 하면 빈 문자열(관련도순)일 때만 sort가 URL에서 생략되고, "date"(최신순)은 정상적으로 URL에 포함된다.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - goToPage에서 `sort !== "date"` 패턴이 제거됨
    - handleCategoryChange에서 `sort !== "date"` 패턴이 제거됨
    - handleSortChange에서 `sort !== "date"` 패턴이 제거됨
    - 3곳 모두 빈 문자열만 생략하는 로직으로 변경됨
    - TypeScript 컴파일 에러 없음
  </acceptance_criteria>
  <done>검색에서 sort="date" 선택 후 페이지네이션/카테고리 변경 시 sort 파라미터가 URL에 유지된다</done>
</task>

<task type="auto">
  <name>Task 2: 태그 페이지 accent 색상 적용 + 페이지네이션 추가 (BUG-03, BUG-05)</name>
  <files>src/app/tag/[name]/page.tsx</files>
  <read_first>src/app/tag/[name]/page.tsx</read_first>
  <action>
태그 페이지에서 두 가지 문제를 수정한다.

**1. accent 변수를 실제로 사용하도록 수정 (BUG-03)**

현재: `accent` 변수를 계산하지만 JSX에서 미사용. 5곳에 `#E8192C`가 하드코딩됨.

아래 5곳의 하드코딩된 `#E8192C`를 `accent` 변수를 사용하는 인라인 style로 교체:

(a) 라인 60 브레드크럼 hover: `hover:text-[#E8192C]` → `style` 속성은 hover에 직접 적용 불가하므로, className에서 `hover:text-[#E8192C]`를 제거하고 CSS 변수 방식 사용:
  - 최상위 `<div>`에 `style={{ "--tag-accent": accent } as React.CSSProperties}`를 추가
  - 5곳 모두 `text-[#E8192C]`를 `text-[var(--tag-accent)]`로, `hover:text-[#E8192C]`를 `hover:text-[var(--tag-accent)]`로 변경

구체적 변경 위치:
- 라인 52 최상위 div: style에 `"--tag-accent": accent` 추가 → `style={{ fontFamily: "...", "--tag-accent": accent } as React.CSSProperties}`
- 라인 60: `hover:text-[#E8192C]` → `hover:text-[var(--tag-accent)]`
- 라인 67: `text-[#E8192C]` → `text-[var(--tag-accent)]`
- 라인 80: `text-[#E8192C]` → `text-[var(--tag-accent)]`
- 라인 113: `text-[#E8192C]` → `text-[var(--tag-accent)]`
- 라인 116: `group-hover:text-[#E8192C]` → `group-hover:text-[var(--tag-accent)]`

**2. 클라이언트 측 페이지네이션 추가 (BUG-05)**

서버 컴포넌트인 태그 페이지에 "더 보기" 방식의 클라이언트 페이지네이션을 추가한다.
현재 서버 컴포넌트이므로, 기사 목록 부분을 클라이언트 컴포넌트로 분리하는 것이 가장 깔끔하지만, 최소 변경 원칙에 따라 더 간단한 방법을 사용한다:

- 서버에서 전체 기사를 가져오되, 기사 목록 렌더링을 별도 클라이언트 컴포넌트 `TagArticleList`로 분리
- `TagArticleList`는 같은 파일 하단에 "use client" 별도 파일로 생성: `src/app/tag/[name]/TagArticleList.tsx`

**TagArticleList.tsx 생성:**
```typescript
"use client";
import { useState } from "react";
import Link from "next/link";
import Image from "next/image";
import type { Article } from "@/types/article";

const PER_PAGE = 20;

export default function TagArticleList({ articles, accent }: { articles: Article[]; accent: string }) {
  const [visibleCount, setVisibleCount] = useState(PER_PAGE);
  const visible = articles.slice(0, visibleCount);
  const hasMore = visibleCount < articles.length;

  return (
    <>
      <div className="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
        {visible.map((article) => (
          // 기존 article 카드 JSX를 여기로 이동 (라인 87-129)
          // #E8192C 하드코딩은 accent prop 사용
        ))}
      </div>
      {hasMore && (
        <div className="mt-8 text-center">
          <button
            onClick={() => setVisibleCount((c) => c + PER_PAGE)}
            className="px-6 py-2.5 border border-gray-300 rounded-lg text-sm text-gray-700 hover:bg-gray-50 transition-colors"
          >
            더 보기 ({articles.length - visibleCount}건 남음)
          </button>
        </div>
      )}
    </>
  );
}
```

**page.tsx 수정:**
- `TagArticleList` import 추가
- 기존 기사 목록 grid (라인 85-130)를 `<TagArticleList articles={articles} accent={accent} />`로 교체
- 카드 내부의 `text-[#E8192C]` 2곳도 accent prop 기반 CSS 변수로 변경
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/tag/[name]/page.tsx에 하드코딩된 `#E8192C`가 0개
    - src/app/tag/[name]/TagArticleList.tsx 파일이 생성됨
    - TagArticleList에 "use client" 선언 존재
    - TagArticleList에 PER_PAGE = 20 상수 존재
    - TagArticleList에 "더 보기" 버튼이 hasMore 조건으로 표시
    - page.tsx에서 accent 변수가 CSS 변수로 전달됨
    - TypeScript 컴파일 에러 없음
  </acceptance_criteria>
  <done>태그 페이지에서 culturepeople 보라색/insightkorea 빨간색이 적용되고, 기사 20건 초과 시 "더 보기" 버튼이 나타난다</done>
</task>

</tasks>

<verification>
pnpm build가 에러 없이 완료된다.
태그 페이지에 하드코딩된 #E8192C가 없다.
검색 페이지에 sort !== "date" 패턴이 없다.
</verification>

<success_criteria>
1. 검색에서 "최신순" 정렬 후 2페이지 클릭 시 URL에 sort=date가 유지된다
2. /tag/xxx 페이지에서 culturepeople 테마일 때 보라색(#5B4B9E) accent가 표시된다
3. /tag/xxx 페이지에서 기사 20건 이상일 때 "더 보기" 버튼이 나타나고 클릭하면 추가 기사가 표시된다
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-pages/02-01-SUMMARY.md`
</output>
