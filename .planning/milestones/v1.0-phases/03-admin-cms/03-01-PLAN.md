---
phase: 03-admin-cms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/cam/settings/page.tsx
  - src/app/cam/articles/[id]/edit/page.tsx
autonomous: true
requirements: [ADM-04, ADM-06]
must_haves:
  truths:
    - "SMTP 비밀번호가 마스킹 상태에서 저장해도 실제 비밀번호가 유지된다"
    - "기사 편집 페이지 진입 시 로딩 인디케이터가 표시되고, 데이터 로드 전 저장 불가"
    - "기사 데이터 로드 직후에는 isDirty가 false이고, 미저장 경고가 뜨지 않는다"
  artifacts:
    - path: "src/app/cam/settings/page.tsx"
      provides: "SMTP 비밀번호 마스킹 값 전송 방지"
      contains: "smtpPass.*마스킹|KEEP"
    - path: "src/app/cam/articles/[id]/edit/page.tsx"
      provides: "로딩 상태 + isDirty 초기화 수정"
      contains: "loading|isLoaded"
  key_links:
    - from: "src/app/cam/settings/page.tsx"
      to: "/api/db/settings"
      via: "saveSetting(cp-newsletter-settings)"
      pattern: "saveSetting.*cp-newsletter"
---

<objective>
CRITICAL/HIGH 버그 2건 수정: SMTP 비밀번호 마스킹 값 저장 위험(BUG-01/10)과 기사 편집 페이지 로딩 상태 미표시(BUG-03) + isDirty 오작동(BUG-07).

Purpose: 데이터 손실 위험 제거 + 편집 UX 안정화
Output: settings/page.tsx, edit/page.tsx 수정
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-admin-cms/03-RESEARCH.md

@src/app/cam/settings/page.tsx
@src/app/cam/articles/[id]/edit/page.tsx
@src/app/api/db/settings/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SMTP 비밀번호 마스킹 값 전송 방지 (BUG-01/10)</name>
  <files>src/app/cam/settings/page.tsx</files>
  <read_first>
    - src/app/cam/settings/page.tsx (전체 — SMTP 저장 흐름 파악)
    - src/app/api/db/settings/route.ts (PUT 핸들러의 마스킹 방어 로직 확인)
  </read_first>
  <action>
settings/page.tsx에서 SMTP 저장 시 마스킹 값("••••••••")이 그대로 전송되는 문제 수정.

현재 상태: PUT API에서 "••••••••"을 감지하여 기존 값 유지하는 방어 로직이 있지만, 클라이언트가 이 방어에 의존하는 것은 불안정.

수정 방법:
1. SmtpSettings 상태 옆에 `smtpPassChanged` (boolean, 초기값 false) 상태를 추가한다
2. smtpPass input의 onChange에서 `setSmtpPassChanged(true)`를 호출한다
3. SMTP 저장 시 (`saveSetting("cp-newsletter-settings", smtp)` 호출부 — line ~1044):
   - `smtpPassChanged === false`이면 smtpPass를 "••••••••"로 유지하여 전송 (PUT API 방어 로직이 기존 값 유지)
   - `smtpPassChanged === true`이면 사용자가 입력한 새 값 전송
4. SMTP 테스트 버튼 (line ~1072)도 동일하게 수정: `smtpPassChanged ? smtp.smtpPass : "__KEEP__"` 패턴 유지
5. 비밀번호 input에 placeholder="저장된 비밀번호 있음" 추가 (마스킹 값 표시 대신)

핵심: 기존 PUT API 방어 로직은 그대로 유지 (이중 방어). 클라이언트에서도 불필요한 마스킹 값 전송을 방지.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - smtpPassChanged 상태가 추가되어 비밀번호 변경 여부를 추적
    - 비밀번호 미변경 시 마스킹 값이 그대로 전송되어 PUT API가 기존 값 유지
    - 비밀번호 변경 시 새 값이 정상 전송
    - 빌드 성공
  </acceptance_criteria>
  <done>SMTP 비밀번호 마스킹 값이 실수로 저장되는 위험이 제거됨</done>
</task>

<task type="auto">
  <name>Task 2: 편집 페이지 로딩 상태 + isDirty 수정 (BUG-03/07)</name>
  <files>src/app/cam/articles/[id]/edit/page.tsx</files>
  <read_first>
    - src/app/cam/articles/[id]/edit/page.tsx (전체 — 로딩 흐름, isDirty useEffect 확인)
  </read_first>
  <action>
BUG-03: 편집 페이지 진입 시 로딩 인디케이터 없이 빈 폼이 표시되는 문제 수정.
BUG-07: 기사 데이터 로드 시 setTitle/setBody 호출로 isDirty가 항상 true가 되는 문제 수정.

수정 방법:

1. `const [pageLoading, setPageLoading] = useState(true)` 상태 추가 (line ~26 부근)
2. getArticleById 콜백 마지막에 `setPageLoading(false)` 추가 (line ~126 부근, setReviewNote 뒤)
3. getArticleById catch에서도 `setPageLoading(false)` 추가
4. 렌더링 부분 (return 시작): `if (pageLoading)` 일 때 로딩 UI 반환:
   ```tsx
   if (pageLoading) return (
     <div style={{ padding: 32, textAlign: "center" }}>
       <div style={{ fontSize: 16, color: "#666" }}>기사 데이터를 불러오는 중...</div>
     </div>
   );
   ```
5. isDirty 수정 — line 166-168의 useEffect를 수정:
   ```tsx
   const isLoadedRef = useRef(false);
   // getArticleById 콜백 마지막에 setTimeout(() => { isLoadedRef.current = true; }, 0) 추가
   useEffect(() => {
     if (isLoadedRef.current && (title || body)) isDirtyRef.current = true;
   }, [title, body]);
   ```
   이렇게 하면 초기 데이터 로드 시에는 isDirty가 false를 유지하고, 이후 사용자 편집 시에만 true가 됨.
6. 저장 버튼에 `disabled={pageLoading || saving || reuploading}` 조건 추가하여 로드 전 저장 방지

핵심: pageLoading 상태로 빈 폼 노출 차단. isLoadedRef로 초기 로드와 사용자 편집을 구분.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - 편집 페이지 진입 시 "기사 데이터를 불러오는 중..." 로딩 표시
    - 데이터 로드 전 저장 버튼 비활성화
    - 데이터 로드 직후 isDirty가 false — 변경 없이 뒤로가기 시 경고 없음
    - 사용자가 제목/본문 편집 후에는 isDirty가 true — 뒤로가기 시 경고 표시
    - 빌드 성공
  </acceptance_criteria>
  <done>편집 페이지에서 빈 폼 노출 및 불필요한 미저장 경고가 제거됨</done>
</task>

</tasks>

<verification>
- `pnpm build` 성공
- settings/page.tsx에서 smtpPassChanged 상태 추가 확인
- edit/page.tsx에서 pageLoading 상태와 isLoadedRef 추가 확인
</verification>

<success_criteria>
- SMTP 비밀번호 마스킹 값 저장 위험 제거 (BUG-01/10)
- 편집 페이지 로딩 상태 표시 + 빈 폼 저장 방지 (BUG-03)
- isDirty 초기 로드 시 false 유지 (BUG-07)
- 빌드 성공
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-cms/03-01-SUMMARY.md`
</output>
