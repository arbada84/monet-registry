---
phase: 04-automation
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/api/cron/auto-press/route.ts
  - src/app/api/cron/auto-news/route.ts
  - src/app/api/mail/sync/route.ts
autonomous: true
requirements: [AUT-02, AUT-03]

must_haves:
  truths:
    - "auto-press가 mail/sync를 self-fetch 대신 직접 함수 호출한다"
    - "IMAP 비밀번호 복호화 실패 시 해당 계정만 건너뛰고 다른 계정은 정상 동기화된다"
    - "auto-press reviewNote가 실제 재시도 횟수(3회)와 일치한다"
    - "auto-news 이미지 없는 기사에 OG 재귀 참조가 발생하지 않는다"
  artifacts:
    - path: "src/app/api/cron/auto-press/route.ts"
      provides: "보도자료 cron (직접 호출, 수정된 reviewNote, keywords 제한)"
      contains: "syncMailAccounts"
    - path: "src/app/api/mail/sync/route.ts"
      provides: "IMAP 동기화 (decrypt 에러 격리, 함수 export)"
      contains: "try.*decrypt"
    - path: "src/app/api/cron/auto-news/route.ts"
      provides: "RSS cron (OG 재귀 방어)"
      contains: "api/og"
  key_links:
    - from: "src/app/api/cron/auto-press/route.ts"
      to: "src/app/api/mail/sync/route.ts"
      via: "직접 함수 import 호출 (self-fetch 제거)"
      pattern: "syncMailAccounts|runMailSync"
---

<objective>
auto-press self-fetch 제거, IMAP decrypt 에러 격리, 기타 중간/낮은 우선순위 버그 4건을 수정한다.

Purpose: BUG-03은 self-fetch로 인한 시간 부족/커넥션 이슈, BUG-08은 한 계정 실패가 전체를 크래시시킴
Output: auto-press, auto-news, mail/sync 3개 파일 수정 + Vercel 배포
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-automation/04-RESEARCH.md
@.planning/phases/04-automation/04-01-SUMMARY.md

<interfaces>
From src/app/api/cron/auto-press/route.ts (line 780-811):
- 체인콜 블록: mail settings 조회 -> autoSync 확인 -> self-fetch POST /api/mail/sync
- 55000ms AbortSignal 타임아웃 설정 (auto-press 자체가 이미 50초+ 소모 후)
- 수정 대상: self-fetch를 mail/sync의 핵심 로직 직접 호출로 교체

From src/app/api/cron/auto-press/route.ts (line 672):
- reviewNote: "AI 편집 실패 — 수동 검토 필요 (3회 재시도 소진)"
- 실제로는 Plan 01에서 3회로 변경되므로 메시지도 3회가 맞음 (이미 일치할 수 있으나 확인 필요)

From src/app/api/cron/auto-press/route.ts (line 753):
- keywords 파싱: `.split(",").map((k) => k.trim())` -- 길이/개수 제한 없음
- auto-news 패턴: `.map((k) => k.trim().slice(0, 50)).filter(Boolean).slice(0, 20)`

From src/app/api/mail/sync/route.ts (line 40-58):
- getAccounts(): `.map((a) => ({ ...a, password: decrypt(a.password) }))` -- decrypt 실패 시 전체 크래시
- 핵심 동기화 로직이 POST handler 내부에 있으므로 직접 호출을 위해 함수 추출 필요

From src/app/api/cron/auto-news/route.ts (line 552-556):
- 이미지 없을 때 OG API URL로 thumbnail 설정 -- 재귀 참조 위험
- 수정: thumbnail 없으면 빈 문자열 유지 (auto-press와 동일하게 이미지 없이 등록)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BUG-08 decrypt 에러 격리 + BUG-03 mail/sync 함수 추출 + auto-press 직접 호출</name>
  <files>src/app/api/mail/sync/route.ts, src/app/api/cron/auto-press/route.ts</files>
  <read_first>
    - src/app/api/mail/sync/route.ts (전체, 248줄)
    - src/app/api/cron/auto-press/route.ts (line 780-820, 체인콜 블록)
  </read_first>
  <action>
**BUG-08 (MEDIUM) -- mail/sync/route.ts line 44-45:**
- `getAccounts()` 내 `.map((a) => ({ ...a, password: decrypt(a.password) }))` 를 안전한 버전으로 교체:
  ```typescript
  .map((a) => {
    try {
      return { ...a, password: decrypt(a.password) };
    } catch {
      console.error(`[mail/sync] 계정 ${a.email} 비밀번호 복호화 실패 -- 건너뜀`);
      return null;
    }
  }).filter((a): a is MailAccountSetting => a !== null)
  ```

**BUG-03 (MEDIUM) -- mail/sync 함수 추출 + auto-press 직접 호출:**
- mail/sync/route.ts: POST handler의 핵심 동기화 로직을 `export async function runMailSync(days: number)` 함수로 추출
  - 인증 체크 부분은 POST handler에 유지
  - 동기화 로직(getAccounts -> IMAP 연결 -> 메일 가져오기 -> 결과 반환)을 별도 함수로
  - POST handler는 인증 후 `runMailSync(days)` 호출하여 결과 반환
- auto-press/route.ts line 780-811: self-fetch 블록 전체를 직접 호출로 교체:
  ```typescript
  // 체인콜: 메일 동기화 (직접 함수 호출)
  if (body.source === "cron" || !body.source) {
    try {
      const mailSettings = await serverGetSetting<{ autoSync?: boolean; autoSyncDays?: number }>("cp-mail-settings", {});
      if (mailSettings.autoSync) {
        const syncDays = mailSettings.autoSyncDays || 1;
        try {
          const { runMailSync } = await import("@/app/api/mail/sync/route");
          const syncResult = await runMailSync(syncDays);
          mailSyncResult = { success: true, ...syncResult };
        } catch (syncErr) {
          const errMsg = syncErr instanceof Error ? syncErr.message : "알 수 없는 오류";
          console.warn("[auto-press] mail sync 실패:", errMsg);
          mailSyncResult = { success: false, error: errMsg };
        }
      }
    } catch { /* 메일 설정 조회 실패는 무시 */ }
  }
  ```
- self-fetch에 사용하던 `fetch()`, `AbortSignal.timeout(55000)`, CRON_SECRET 헤더 설정 코드 제거
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - mail/sync에서 decrypt가 try/catch로 감싸져 있다
    - mail/sync에서 runMailSync 함수가 export되어 있다
    - auto-press에서 `fetch(.*mail/sync` 패턴이 없다 (self-fetch 제거)
    - auto-press에서 runMailSync를 import하여 직접 호출한다
  </acceptance_criteria>
  <done>IMAP 복호화 실패가 격리되고, auto-press가 mail/sync를 직접 함수 호출한다</done>
</task>

<task type="auto">
  <name>Task 2: BUG-05 reviewNote + BUG-06 OG 재귀 방어 + BUG-07 keywords 제한 + 빌드/배포</name>
  <files>src/app/api/cron/auto-press/route.ts, src/app/api/cron/auto-news/route.ts</files>
  <read_first>
    - src/app/api/cron/auto-press/route.ts (line 670-675, reviewNote)
    - src/app/api/cron/auto-press/route.ts (line 750-755, keywords 파싱)
    - src/app/api/cron/auto-news/route.ts (line 550-560, OG 이미지)
  </read_first>
  <action>
**BUG-05 (LOW) -- auto-press/route.ts line 672:**
- Plan 01에서 AI가 3회 시도로 변경되므로, reviewNote 메시지 확인:
- 현재: `"AI 편집 실패 — 수동 검토 필요 (3회 재시도 소진)"` -- 이미 3회라면 그대로 유지
- 만약 여전히 다른 숫자라면 `"AI 편집 실패 — 수동 검토 필요 (3회 재시도 소진)"`으로 통일
- auto-news에도 동일한 reviewNote가 있으면 일치하는지 확인

**BUG-06 (MEDIUM) -- auto-news/route.ts line 552-556:**
- OG API URL 재귀 참조 방어: thumbnail 없는 기사에 `/api/og?id=` URL을 설정하는 코드 블록 제거
- 대신 thumbnail 없이 기사 등록 (auto-press와 동일한 패턴)
- 즉, line 551-556의 `if (!thumbnail)` 블록과 `serverUpdateArticle` import를 제거
- 이유: OG API가 thumbnail을 참조하여 이미지를 생성하므로 재귀 참조 위험. thumbnail이 없으면 OG API가 기본 사이트 이미지를 사용하도록 하는 것이 안전

**BUG-07 (LOW) -- auto-press/route.ts line 753:**
- 현재: `sp.get("keywords")!.split(",").map((k) => k.trim())`
- 변경: `sp.get("keywords")!.split(",").map((k) => k.trim().slice(0, 50)).filter(Boolean).slice(0, 20)`
- auto-news와 동일한 제한 패턴 적용 (최대 20개, 각 50자)

**배포:**
- `pnpm build` 성공 확인 후 `vercel deploy --prod` 실행
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - auto-news에서 `/api/og?id=` URL을 thumbnail로 설정하는 코드가 없다
    - auto-press keywords에 `.slice(0, 50)` 와 `.slice(0, 20)` 제한이 있다
    - auto-news와 auto-press의 reviewNote 메시지가 일치한다
    - pnpm build 성공
    - vercel deploy --prod 성공
  </acceptance_criteria>
  <done>OG 재귀 참조 방어, keywords 제한, reviewNote 일치, Vercel 배포 완료</done>
</task>

</tasks>

<verification>
1. `pnpm build` 성공
2. `grep -rn "fetch.*mail/sync" src/app/api/cron/auto-press/` -- self-fetch 없음
3. `grep -rn "api/og?id=" src/app/api/cron/auto-news/` -- OG 재귀 참조 없음
4. `grep -rn "decrypt(a.password)" src/app/api/mail/sync/` -- try/catch 내부에 있음
5. Vercel 프로덕션 배포 성공
</verification>

<success_criteria>
- BUG-03 해결: auto-press가 mail/sync를 직접 함수 호출
- BUG-05 해결: reviewNote 재시도 횟수 일치
- BUG-06 해결: OG 이미지 재귀 참조 방어
- BUG-07 해결: auto-press keywords 길이/개수 제한
- BUG-08 해결: IMAP decrypt 실패 시 개별 계정 격리
- Vercel 프로덕션 배포 완료
</success_criteria>

<output>
After completion, create `.planning/phases/04-automation/04-02-SUMMARY.md`
</output>
