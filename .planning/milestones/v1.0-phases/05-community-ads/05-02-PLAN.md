---
phase: 05-community-ads
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/db/comments/route.ts
  - supabase-schema.sql
autonomous: true
requirements: [COM-01, COM-02, COM-05, COM-06]

must_haves:
  truths:
    - "부모 댓글 삭제 시 자식 답글도 함께 삭제되어 고아 댓글이 발생하지 않음"
    - "댓글 POST에서 articleId가 빈 문자열이나 비정상 값이면 400 반환"
    - "supabase-schema.sql이 실제 comments 테이블 구조와 일치"
    - "광고 시스템(AdSense, 쿠팡)이 정상 동작 확인됨 (리서치에서 정상 확인)"
  artifacts:
    - path: "src/app/api/db/comments/route.ts"
      provides: "댓글 CRUD API (부모 삭제 시 자식 연쇄 삭제)"
      contains: "parent_id"
    - path: "supabase-schema.sql"
      provides: "스키마 정의 파일 (실제 DB와 동기화)"
      contains: "parent_id"
  key_links:
    - from: "src/app/api/db/comments/route.ts"
      to: "Supabase comments 테이블"
      via: "REST API fetch"
      pattern: "rest/v1/comments"
---

<objective>
댓글 시스템의 엣지 케이스 버그(부모 삭제 시 고아 답글, articleId 검증 누락)와 스키마 파일 불일치를 수정한다. 광고 시스템은 리서치에서 정상 확인되었으므로 빌드/배포 시 동작을 확인한다.

Purpose: BUG-C04(MEDIUM), BUG-C06(LOW), BUG-C01(LOW) 해결 + COM-05/COM-06 정상 확인
Output: 수정된 댓글 API + 동기화된 스키마 파일
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-community-ads/05-RESEARCH.md
</context>

<interfaces>
<!-- 댓글 API에서 사용하는 기존 타입/함수 -->
From src/types/article.ts:
```typescript
export interface Comment {
  id: string;
  articleId: string;
  articleTitle?: string;
  author: string;
  content: string;
  createdAt: string;
  status: "approved" | "pending" | "spam";
  ip?: string;
  parentId?: string | null;
}
```

From src/app/api/db/comments/route.ts (기존 헬퍼):
```typescript
function sbHeaders(write?: boolean): Record<string, string>;
async function isTableMode(): Promise<boolean>;
function rowToComment(r: Record<string, unknown>): Comment;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 댓글 DELETE 연쇄 삭제 + POST articleId 검증 (BUG-C04, BUG-C06)</name>
  <files>src/app/api/db/comments/route.ts</files>
  <read_first>
    - src/app/api/db/comments/route.ts (전체 — 특히 DELETE 284-314행, POST 165-200행)
  </read_first>
  <action>
    src/app/api/db/comments/route.ts를 수정한다:

    **BUG-C04 (MEDIUM — 부모 삭제 시 자식 고아화):** DELETE 핸들러의 테이블 모드 분기(294-301행)에서 부모 삭제 전에 자식 답글을 먼저 삭제한다.
    - 기존: `id=eq.${id}`만 삭제
    - 변경: 2단계 삭제 — 먼저 `parent_id=eq.${id}`로 자식 삭제, 그 다음 `id=eq.${id}`로 부모 삭제.
    - 코드:
      ```typescript
      // 1) 자식 답글 먼저 삭제
      await fetch(`${SB_URL}/rest/v1/comments?parent_id=eq.${encodeURIComponent(id)}`, {
        method: "DELETE",
        headers: { ...sbHeaders(true), Prefer: "return=minimal" },
      });
      // 2) 부모 댓글 삭제
      const res = await fetch(`${SB_URL}/rest/v1/comments?id=eq.${encodeURIComponent(id)}`, {
        method: "DELETE",
        headers: { ...sbHeaders(true), Prefer: "return=minimal" },
      });
      ```
    - JSON 폴백(304-308행)도 동일하게 수정: 삭제 대상 id의 자식(parentId === id)도 함께 필터링.
      ```typescript
      const updated = all.filter((c) => c.id !== id && c.parentId !== id);
      ```

    **BUG-C06 (LOW — articleId UUID 검증 누락):** POST 핸들러 167행에서 articleId의 빈 문자열/과도한 길이 방어를 추가한다. UUID 형식 강제는 하지 않는다 (article_id가 TEXT 타입이고, 기존 기사 중 UUID가 아닌 것이 있을 수 있으므로).
    - 변경: `if (!articleId || typeof articleId !== "string"` 뒤에 `|| articleId.length > 200` 조건 추가.
    - articleId에도 기본 sanitize 적용: `articleId.trim()`이 빈 문자열이면 400 반환.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - DELETE 테이블 모드에서 parent_id=eq.{id}로 자식 삭제 후 id=eq.{id}로 부모 삭제
    - DELETE JSON 폴백에서 c.parentId !== id 조건 추가
    - POST에서 articleId 길이 200자 초과 시 400 반환
    - pnpm build 성공
  </acceptance_criteria>
  <done>부모 댓글 삭제 시 자식 답글이 함께 삭제되고, articleId 검증이 강화됨</done>
</task>

<task type="auto">
  <name>Task 2: 스키마 파일 동기화 + 빌드 검증 + 배포 (BUG-C01)</name>
  <files>supabase-schema.sql</files>
  <read_first>
    - supabase-schema.sql (comments 테이블 정의 부분)
    - src/app/api/admin/migrate-comments/route.ts (실제 테이블 생성 SQL 33-55행)
  </read_first>
  <action>
    **BUG-C01 (LOW — 스키마 파일 불일치):** supabase-schema.sql의 comments 테이블 정의를 실제 마이그레이션 스크립트(migrate-comments/route.ts 33-44행)와 일치시킨다.

    현재(28-35행):
    ```sql
    CREATE TABLE IF NOT EXISTS comments (
      id TEXT PRIMARY KEY,
      article_id TEXT REFERENCES articles(id) ON DELETE CASCADE,
      author TEXT NOT NULL,
      content TEXT NOT NULL,
      status TEXT NOT NULL DEFAULT 'pending',
      created_at TIMESTAMPTZ DEFAULT now()
    );
    ```

    변경 후:
    ```sql
    CREATE TABLE IF NOT EXISTS comments (
      id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
      article_id TEXT NOT NULL,
      article_title TEXT,
      author TEXT NOT NULL,
      content TEXT NOT NULL,
      created_at TIMESTAMPTZ NOT NULL DEFAULT now(),
      status TEXT NOT NULL DEFAULT 'pending' CHECK (status IN ('approved','pending','spam')),
      ip TEXT,
      parent_id UUID REFERENCES comments(id) ON DELETE SET NULL
    );
    CREATE INDEX IF NOT EXISTS idx_comments_article ON comments(article_id);
    CREATE INDEX IF NOT EXISTS idx_comments_status ON comments(status);
    ```

    **빌드 검증:** `pnpm build` 성공 확인.
    **배포:** `vercel deploy --prod`로 프로덕션 배포.

    **COM-05/COM-06 (광고 확인):** 리서치에서 AdSense 자동광고와 쿠팡 추천 상품이 정상 동작 확인됨. 코드 변경 불필요. 배포 후 사이트 접속으로 광고 표시 확인 가능.
  </action>
  <verify>
    <automated>cd D:/Users/Documents/monet-registry-main && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - supabase-schema.sql의 comments 테이블이 id UUID, article_title, ip, parent_id 컬럼을 포함
    - CHECK 제약 조건과 인덱스가 포함됨
    - pnpm build 성공
    - vercel deploy --prod 완료
  </acceptance_criteria>
  <done>스키마 파일이 실제 DB와 동기화되고, 댓글+광고 수정이 프로덕션 배포됨</done>
</task>

</tasks>

<verification>
- pnpm build 성공
- comments route.ts DELETE에서 parent_id 자식 삭제 로직이 존재함을 grep으로 확인
- supabase-schema.sql에 parent_id, article_title, ip 컬럼이 존재함을 확인
- vercel deploy --prod 성공
</verification>

<success_criteria>
- BUG-C04: 부모 댓글 삭제 시 자식 답글도 함께 삭제됨
- BUG-C06: articleId 길이 검증이 추가됨
- BUG-C01: supabase-schema.sql이 실제 DB 구조와 일치
- COM-01/COM-02: 댓글 CRUD와 답글이 정상 동작 (기존 기능 유지 + 버그 수정)
- COM-05/COM-06: 광고 시스템 정상 확인 (코드 변경 없음)
- 프로덕션 배포 완료
</success_criteria>

<output>
After completion, create `.planning/phases/05-community-ads/05-02-SUMMARY.md`
</output>
